# SYSTEM v4.1.6 — Patch Notes (hygiene-only)

**Cycle:** 2026-08-17. retrieval_ref `LM-RET-2026-08-17T01:50Z-B` (orchestration-memory@b62df0a539c8, preferences@fea4f809c4c2, lessons L-20260817-01/02/03) — recorded on the task and echoed by the patch unit.
**Routing:** Fable 5 orchestrated and gated; one Opus 5 unit executed both files (1 auto-return bounce). Grok not called.
**Snapshot → Verify → Rollback:** sources untouched (sha256 verified before and after, twice); patch landed as new files; rollback = delete `patch-v4.1.6/`. Staged docs only — no live path was authorized and none was used.

## CHANGELOG (line-level)

**SYSTEM-v4.1.6-FOR-CLAUDE.md** (from v4.1.5; 10 lines replaced + 2 inserted, all other lines byte-identical, confirmed by diff):
- L1 title → v4.1.6
- L70 Hard Rule 7 → enforcement clause restored: "A missing retrieval reference is a self-test failure" (SPEC-intent wording) [fix 2]
- L104 / L118 / L140 / L151 → weekly reset now "Sunday 9:00 PM America/New_York" at all four sites, one identical rendering, zero stale "9pm" [fix 5]
- L120 enum → `[NORMAL | CONSERVE_CLAUDE | CLAUDE_CRITICAL | CONSERVE_GROK | GROK_DOWN | CONSERVE_BOTH | UNKNOWN]`; STOP_LONG_RUNS removed (an action, redundant with CONSERVE_BOTH — logged in the v4.1.6 history row) [fix 3]
- L130 Unknown row → Mode cell is the real token UNKNOWN; condition moved to Action ("Treat as CONSERVE_CLAUDE if Owner reported high usage; browser-check before any long Claude run") [fix 3]
- L151 standing order → steps (4)/(5) mapped to enum "(mode: CLAUDE_CRITICAL)" / "(mode: CONSERVE_BOTH)"; L154 → "record UNKNOWN (mode: UNKNOWN)" [fix 3]
- History → v4.1.3 row marked "cost caveat superseded at v4.1.5 by the role-based cost note" [fix 6]; **v4.1.4 row inserted** attributing Credit-Aware Routing (browser checks, thresholds, Grok Build delegation strategies, Sunday reset, standing order) [fix 1]; v4.1.5 row unchanged; v4.1.6 row added, carries "(this document)" after the dash so the linter parses it

**SYSTEM-v4.1.6-SPEC.md** (from v4.1.3 SPEC; 11 lines replaced + 3 inserted):
- L1–3 header → v4.1.6, 2026-08-17, hygiene-only intro
- L8 Design Goal → "coordinated by Claude Code (Claude Fable 5 Max/Ultracode orchestrates; Claude Opus 5 Max/Ultracode executes all other Claude work)" [fix 4]
- L36–37 Main Coordinator → split heading + Owner model-split line (role-based, not cost-based; Fable priced higher), replacing the obsolete "Fable = non-critical fallback" line
- L103 Hard Rule 1 → matches FOR-CLAUDE: Claude Code surface holds live-write authority (orchestrator Fable, other work Opus)
- L127–129 Model Routing → Cowork row caveat updated to role-based framing; Fable = orchestration/verification/live-write authority surface; Opus = all other Claude work; Discovery row untouched
- History → "(this document)" removed from v4.1.3 row + superseded note added [fixes 6, 7]; v4.1.4, v4.1.5, v4.1.6 rows added; exactly one "(this document)", on v4.1.6, linter-parseable [fix 7]

## specguard evidence (before → after)

- FOR-CLAUDE: **13 findings [10 FAIL, 3 ADVISORY] → 3 [2 FAIL, 1 ADVISORY]**. Cleared: v4.1.4 numbering gap, both enum mismatches, all 4 timezone-less reset sites, reset-format inconsistency, Credit-Aware-Routing and Hard-rules attribution advisories. Version run now parses v1→v4.1.6 contiguously, zero unparsed history rows.
- SPEC: **3 findings [0 FAIL] → 2 [0 FAIL]**; Design Goal advisory cleared; version run parses v1→v4.1.6, no gaps.
- Full outputs: `specguard-before.txt` / `specguard-after.txt`; source hashes: `snapshot-hashes.txt`.

## DONE MEANS — verified item by item at the gate

v4.1.4 present in FOR-CLAUDE history ✓ · HR7 fail-on-missing-retrieval_ref present ✓ · enum covers every threshold mode (7/7, both directions) ✓ · Design Goal matches Fable/Opus split ✓ · reset carries America/New_York (4/4 sites) ✓ · remaining gaps listed below ✓

## OPEN ITEMS after this patch (not falsely green — these remain)

1. **Pooled Fable/Opus budget** (FOR-CLAUDE L118 template line + L124 shared threshold table): two separately-budgeted models still report as one figure — "Fable exhausted, Opus fine" is inexpressible, and a note written from the template cannot pass `specguard --check-note` (no dedicated per-model lines). Not ordered in this patch; needs an Owner decision on split reporting.
2. **Credit-Aware Routing lives only in the FOR-CLAUDE package** — the SPEC now logs it (v4.1.4 row) but does not mirror the section body; the SPEC stays silent on thresholds, the standing order, and the reset.
3. **SPEC team table (L48–52)** still uses pre-split phrasing ("Claude Code (Opus/Fable)", "Claude Code + Opus 5") — not ordered; now stylistically inconsistent with the rebound Hard Rule 1 and routing table.
4. **v4.1.5 condensation drops** flagged last cycle remain unrestored in FOR-CLAUDE (UNVERIFIED→verified promotion path owner, "raise an Issue" on 3 fails, "never an authority", non-significant carve-out, worktrees/Grok-Build bridge as shared infra) — not ordered.
5. **Write-safety auditability**: Hard Rules 1/2/4 remain honor-system (no write-ledger, no actor/model attribution on writes).
6. **Self-test pass criteria and the 3-fail counter scope** remain undefined in both docs.
7. **Grok "low" is unquantified**, so the CONSERVE_BOTH trigger stays a judgement call; the reset moment is transcribed from the Owner directive, not verified against the provider UI.
8. **Heuristic advisories**: Hierarchy (FOR-CLAUDE) and Layers (SPEC) unattributed in history — keyword-heuristic noise, listed for completeness.
9. **Deferred by directive, unchanged**: Event Bus duplicate-collapse rule (Phase 2); Grok Bot Layer (deferred, not cancelled).

## Lesson written (process defect this patch exposed)

**L-20260817-04** — Multi-file source-of-truth drift: the FOR-CLAUDE package advanced two versions (v4.1.4, v4.1.5) while the SPEC sat at v4.1.3, and nothing flagged the divergence until a manual review. Every version row must land in every file of the package in the same patch; a cross-file version-sync check belongs in specguard (proposed, not built — out of scope this cycle). Corollary fixed this cycle: the "(this document)" marker placed before the em dash made the newest history row invisible to the numbering check — the newest row is exactly the one that must be machine-checked.

## v4.1.7 addendum (2026-08-17, storage promotion)

- ADDED: "## Durable storage rule" section in SYSTEM-CURRENT.md and SYSTEM-SPEC-CURRENT.md (AUTONOMY-SYSTEM shared durable home; product-local staging temporary; runs incomplete until artifacts promoted; superseded files to delete-me/). Logged as v4.1.7 history rows in both files — no unattributed sections (L-20260817-01), no package drift (L-20260817-04).
- Open items: all 9 v4.1.6 items above stand unchanged; plus specguard still lacks a cross-file version-sync check.

## v4.1.8 addendum (2026-08-17, git + Actions wiring)

- ADDED: "## Shared visibility + version control" section in both SYSTEM docs (GitHub repo `autonomy-system` canonical; Drive folder 1E-0tL4DGXk-HVYNlWUc6ccF6SzZh60OE shared mirror; run incomplete until commit+tag, Drive sync, and SHA+link in Summary Report; no secrets in repo). Logged as v4.1.8 rows in both files.
- ADDED: .github/workflows/sync-docs-to-drive.yml + tools/drive_sync.py (upsert docs/ to the Drive folder; fails with BLOCKED_ON_OWNER_SECRETS message until GOOGLE_SERVICE_ACCOUNT_JSON secret exists). Repo README carries the service-account setup steps.
- Open items: all prior items stand; Actions sync BLOCKED_ON_OWNER_SECRETS until the Owner creates the Google service account + GitHub secret.

## v4.1.9 addendum (2026-08-17, SPEC credit-body mirror)

**What changed**

- ADDED: full `## Credit-Aware Routing (browser checks)` section in SYSTEM-SPEC-CURRENT.md (58 lines, inserted between `## Model Routing` and `## Deferred` — the position analogous to where the FOR-CLAUDE package carries it, and never after Version History). Substance mirrored from SYSTEM-CURRENT.md: CREDIT-CHECK note posted by Cowork to the Event Bus as UNVERIFIED, both threshold tables, the full seven-value Recommendation enum `[NORMAL | CONSERVE_CLAUDE | CLAUDE_CRITICAL | CONSERVE_GROK | GROK_DOWN | CONSERVE_BOTH | UNKNOWN]`, the six Grok Build delegation strategies (volume/critique/planning offload to the Grok stack, batch don't stream, verify-before-apply, fail soft), the standing order for Cowork, and the weekly reset "Sunday 9:00 PM America/New_York" at all four in-section sites, byte-identical to the FOR-CLAUDE rendering.
- ADDED: v4.1.9 history rows in BOTH package files in the same patch (L-20260817-04). "(this document)" moved from the v4.1.8 row to the v4.1.9 row in each file — after the em dash, so the newest row still parses (one marker per file, verified).
- UNCHANGED: SYSTEM-CURRENT.md keeps its Credit-Aware Routing section and every other body section byte-identical; its only edit is the two history lines. No architecture change, no Grok Bot activation, no Event Bus collapse rule invented.
- FOLLOW-UP (same cycle, second commit): title headers bumped to v4.1.9 in both files (SPEC title + intro line, FOR-CLAUDE title). The patch unit bumped history rows but not titles; the Fable gate greps also missed it — caught at Drive-mirror verification. specguard has no title-vs-history check; candidate for the cross-file sync check (open item 2).
- Voice-only adaptations (SPEC style, no substance change): prose intro instead of a `### Purpose` heading; "Snapshot → Verify → Rollback" written in full in delegation strategy 1 where the FOR-CLAUDE copy abbreviates it to "Snapshot → Rollback" (same gate, and the SPEC's established phrasing); "below 5% remaining" for "< 5% remaining" in the standing order.

**Why**

Open item 2 from the v4.1.6 patch — Credit-Aware Routing lived only in the FOR-CLAUDE package while the SPEC logged it in history (v4.1.4 row) and stayed silent on thresholds, the standing order, and the reset. Root cause is L-20260817-04 (multi-file source-of-truth drift): every version row must land in every package file in the same patch. This cycle also shows the sharper form of that defect — a *body section* can be missing from a sibling file while both files lint clean, which is why L-20260817-06 was written.

**specguard evidence (before → after)**

- SYSTEM-SPEC-CURRENT.md: **2 findings [0 FAIL, 2 ADVISORY] → 1 [0 FAIL, 1 ADVISORY]**. `credit-section-missing` cleared; mode/enum checks now actually run on the SPEC and pass (enum 7/7 against declared threshold modes, identical set and spelling to SYSTEM-CURRENT). Remaining: the pre-existing `section-not-in-history` advisory on 'Layers' (keyword heuristic, unchanged). Version run parses v1 → v4.1.9 contiguously.
- SYSTEM-CURRENT.md: **3 findings [2 FAIL, 1 ADVISORY] → 3 [2 FAIL, 1 ADVISORY]** — identical baseline and after (the two `model-budget-collapsed` FAILs on the pooled template line and the pooled threshold heading, plus the 'Hierarchy' advisory). No new failures. Version run parses v1 → v4.1.9 contiguously.
- Net: zero new FAILs in either file; one advisory cleared.

**NOTE — pooled Fable/Opus budget (open item 1, deliberately NOT fixed here)**

The mirror faithfully reproduces the pooled wording, so `Claude Fable/Opus weekly: ~XX% used (~YY% remaining)` and `### Claude thresholds (Fable / Opus weekly)` now exist at a **second site** (SPEC) as well as in FOR-CLAUDE. specguard does **not** raise `model-budget-collapsed` on the SPEC copy: `check_model_budget` only runs when the document has an H2 section whose title contains "model split", and the SPEC keeps the Owner model split as an H3 bullet under Layers → Main Coordinator. So the SPEC's pooled line is unlinted, not clean. Accepted residual for this hygiene patch, recorded rather than papered over; fixing it means splitting per-model reporting, which still needs an Owner decision.

## REMAINING OPEN ITEMS after v4.1.9 (not falsely green — these remain)

CLOSED this cycle: v4.1.6 open item 2 (Credit-Aware Routing body missing from the SPEC).

1. **Pooled Fable/Opus budget** — two separately-budgeted models still report as one figure in the CREDIT-CHECK template and the shared threshold table; "Fable exhausted, Opus fine" is inexpressible and a note written from the template cannot pass `specguard --check-note`. Now duplicated at a second site (SPEC) by the v4.1.9 mirror, and unlinted there (see NOTE above). Needs an Owner decision on split reporting; not ordered.
2. **specguard cross-file version-sync check** — still **proposed, not built**. This cycle proves it must also compare *section presence and content* across package files, not just version rows: both files linted clean while the SPEC was missing a 58-line body section (L-20260817-06). *Owner/Exit:* governed by the authoritative v4.1.11 list below (item 2, as worded 2026-08-18) — this legacy entry is kept for the record only.
3. **SPEC team table (L48–52)** still uses pre-split phrasing ("Claude Code (Opus/Fable)", "Claude Code + Opus 5") — stylistically inconsistent with the rebound Hard Rule 1 and routing table; not ordered.
4. **v4.1.5 condensation drops** remain unrestored in FOR-CLAUDE (UNVERIFIED→verified promotion path owner, "raise an Issue" on 3 fails, "never an authority", non-significant carve-out, worktrees/Grok-Build bridge as shared infra) — not ordered.
5. **Write-safety auditability**: Hard Rules 1/2/4 remain honor-system (no write-ledger, no actor/model attribution on writes). *Owner/Exit:* governed by the authoritative v4.1.11 list below (item 5, as worded 2026-08-18) — this legacy entry is kept for the record only.
6. **Self-test pass criteria and the 3-fail counter scope** remain undefined in both docs. *Owner/Exit:* governed by the authoritative v4.1.11 list below (item 6, as worded 2026-08-18) — this legacy entry is kept for the record only.
7. **Grok "low" is unquantified**, so the CONSERVE_BOTH trigger stays a judgement call; the reset moment is transcribed from the Owner directive, not verified against the provider UI. The mirror copies this imprecision into the SPEC unchanged.
8. **Heuristic advisories**: Hierarchy (FOR-CLAUDE) and Layers (SPEC) unattributed in history — keyword-heuristic noise, listed for completeness.
9. **Deferred by directive, unchanged**: Event Bus duplicate-collapse rule (Phase 2, no rule invented here); Grok Bot Layer (deferred, not cancelled, not activated here).
10. ~~Actions sync BLOCKED_ON_OWNER_SECRETS~~ — CLOSED earlier on 2026-08-17, before this patch: the sync-docs-to-drive workflow is LIVE on Owner OAuth (GOOGLE_OAUTH_CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN repo secrets; Drive-visible create proof SYNC-SMOKE.txt), with the service-account path retained as update-only fallback. Listed because the v4.1.6-era open-items list above still names it; kept here so the closure is on the record, not silently dropped.

## Lesson written this cycle

**L-20260817-06** — A section that exists as a body in one package file but only as a history mention in its sibling is drift the current linter cannot see: specguard lints one file at a time, so the SPEC's missing Credit-Aware Routing body registered only as an ADVISORY while the FOR-CLAUDE copy looked fine. Mirroring must copy substance verbatim (enum tokens, threshold rows, timezone string), not paraphrase, and the proposed cross-file check must compare section presence, not just version rows.

## System health smoke — 2026-08-17

retrieval_ref `LM-RET-2026-08-17T23:12Z-C` (orchestration-memory@81a2c4cbccb1, repo@48559ee, SYSTEM-CURRENT L10/L96–100, PATCH-NOTES tail) — Fable gate, echoed by Opus unit.

- [x] GitHub canonical readable — ls-remote HEAD 48559ee from cloud sandbox 23:12Z; push was proxy-blocked at run start (session source grant pending); actual push result = this commit's own presence on main
- [x] Drive mirror reachable — folder 1E-0tL4DGXk-HVYNlWUc6ccF6SzZh60OE, PATCH-NOTES-CURRENT.md (10roA9WnUm17NL22cmONJKsnFT4L9EZS2) modified 2026-08-17T23:00:06Z
- [x] Actions sync path present — sync-docs-to-drive.yml (push:main, paths docs/**), last green run 32077957124; THIS run's outcome goes in the Summary Report, not pre-claimed here
- [x] Fable/Opus routing stated — SYSTEM-CURRENT hierarchy L10 + routing table L96–100 (role-based split)
- [x] Grok Heavy critique path exercised — grok.com "Heavy — Team of Experts · Grok 4.5", chat "Smoke Path Risks and Fragile Claims" in Owner's Grok history, 9 bullets received, 3 applied
- [x] Open items still listed bluntly — v4.1.9's REMAINING OPEN ITEMS list (10 items) above stands unchanged; none closed, none added, by this smoke

Routing this smoke: Fable gated/routed only; one Opus unit executed (one infra bounce); one Grok Heavy critique call.

## System health smoke (second pass) — 2026-08-18

retrieval_ref `LM-RET-2026-08-18T00:04Z-D` (orchestration-memory@9d8202ad0a1a, repo@515990b, first-pass section + SYSTEM-CURRENT L10/L96–100) — Fable gate, echoed by Opus unit.

- [x] GitHub canonical readable — fetch origin/main = 515990b at 00:04Z from cloud sandbox (the session-push 403 is a standing platform constraint — source binding happens at session creation — not a finding of this smoke); landing path = Owner one-click; actual result = this commit's presence on main
- [x] Drive mirror verified current — PATCH-NOTES-CURRENT.md (10roA9WnUm17NL22cmONJKsnFT4L9EZS2) modified 2026-08-17T23:44:28Z carrying the first-pass section verbatim (checked via Drive API 00:06Z); this pass's sync must advance that timestamp
- [x] Actions sync path proven this evening — first-pass run green in 29s with observed Drive write-through; this commit's own run is the re-verification
- [x] Fable/Opus routing stated — SYSTEM-CURRENT hierarchy L10 + routing table L96–100, unchanged at v4.1.9
- [x] Grok Heavy critique path exercised — "Heavy — Team of Experts · Grok 4.5", chat "Second Smoke Critique Reveals Failures" in Owner's Grok history, 10 bullets received, 3 applied (constraint/finding split; owner+exit added; theater objection logged)
- [x] Open items — v4.1.9's 10 stand unchanged; ONE ADDED this pass: desktop add-repo-to-existing-session picker is defective (OAuth registration errors refs ofid_c89a3ea091e63676 / ofid_6905a097c17ba275, then "Connection to server failed" ×3, 2026-08-17 eve, app 1.32352.0; status page green throughout — app-side flow, not platform); workaround = attach repo at session creation, or Owner-local git; Owner: Chris (report to Anthropic support with both ofid refs); exit: repo successfully added to an EXISTING session

Routing this smoke: Fable gated/routed only; one Opus unit; one Grok Heavy call (second of the evening). Grok challenged same-evening repeat value as checkbox theater — logged; claimed info gain this pass = picker defect recorded with owner/exit + loop repeatability under one-click landing.

## Landing protocol addendum — 2026-08-18

retrieval_ref `LM-RET-2026-08-18T00:04Z-D` carried forward — Fable gated, Opus executed. Owner order: the system must work end to end from any session without effort from him.

- ADDED `tools/land.ps1` + `docs/LANDING-PROTOCOL.md` — the three landing tiers, ff-only throughout, no force-push anywhere.
- Standing clone `C:\Users\chris\autonomy-system-live` is now infrastructure, not a one-off: Owner's cached Git Credential Manager credentials, clone + ls-remote verified 2026-08-17 evening.
- Sandbox-proxy credential probe recorded: the proxy refuses a non-attached repo BEFORE reading credentials (403 from the proxy on a dummy token, not 401 from GitHub) — an externally supplied token cannot buy a push. The repo must be attached at session creation.
- Manual .bat clicking is retired to Tier 3 fallback. Claude performs the landing itself whenever one is needed.
- This very commit is the first zero-touch landing (Tier 2) — no Owner action. No package version change: infra-only, precedent = the OAuth sync commits.

## Loop test — native push (2026-08-18)

retrieval_ref `LM-RET-2026-08-18T01:16Z-Q` (lessons L-20260817-01…06 all read; run-journal tail through 01:20 infra_landing_fix) — Fable gate, Opus unit draft.

- Purpose: prove NATIVE PUSH to origin/main from a web-created, repo-attached session — the exit criterion recorded in the roadmap (record 34) and the standing platform constraint noted in the 2026-08-18 smoke (source binding happens at session creation). Session branch env is present, but the Owner task orders push to main.
- State at run start: base = c6e632a, origin/main identical; specguard baseline unchanged (SYSTEM-CURRENT 3 findings [2 FAIL, 1 ADVISORY] accepted residuals; SPEC 1 [0 FAIL, 1 ADVISORY]).
- Push outcome not claimed here; journal + Summary Report carry it after observation — no Actions run and no Drive sync is asserted by this section.
- Open items: v4.1.9's list (10) plus the smoke-added desktop picker defect stand unchanged by this test — none closed, none added.

## v4.1.10 addendum — 2026-08-18 (Grok CLI bridge)

retrieval_ref `LM-RET-2026-08-18T00:04Z-D` carried forward — Fable gated, Opus executed. Owner order (Chris, 2026-08-18): make the local Grok CLI the DEFAULT Grok Heavy critique path, browser fallback.

- Both package files: titles → v4.1.10 (SPEC **Date:** → 2026-08-18 and intro line with it); one identical `### Grok CLI bridge (default critique path)` block added — after the Model routing table in SYSTEM-CURRENT, under Shared Infrastructure (beneath the Grok Build bridge entry) in the SPEC. Block verified byte-identical across the two files (cmp clean, sha256 71daf2e83bbc878c…).
- History: exactly one "(this document)" per file, moved to the new v4.1.10 row after the em dash (L-20260817-04); the v4.1.9 rows stand intact.
- CLI facts as committed: `grok.exe` 1.0.3 "Grok Build TUI" at `C:\Users\chris\.grok\bin`, logged in with grok.com; one-shot `grok -m grok-4.5 -p "<critique prompt>"` prints the response to stdout and exits; models grok-4.6 (CLI default) and grok-4.5; live round-trip verified 2026-08-18.
- Browser automation to grok.com — the path used in both smoke passes 2026-08-17/18, slow and with one typing misfire — is demoted to FALLBACK, for when the CLI or the Owner desktop is unavailable. Grok output still lands UNVERIFIED with no write authority; Hard Rules unchanged.
- specguard before → after: SYSTEM-CURRENT 3 findings [2 FAIL, 1 ADVISORY] → 3 [2 FAIL, 1 ADVISORY]; SPEC 1 [0 FAIL, 1 ADVISORY] → 1 [0 FAIL, 1 ADVISORY]; `--self-test` 14/14 PASS. Zero new findings; residuals are the known pooled Fable/Opus FAILs and the two keyword-heuristic advisories.
- Critique outcome: 10 bullets returned **through the new CLI path itself** (94s, exit 0 — the call is its own proof); 3 applied to the mechanism block — transport and actor named (orchestrating Claude session via the Owner-machine local shell bridge, same channel as Tier 2 landings), grok-4.5 = Heavy 4.5 tier equivalence flagged as assumed not verified, fallback trigger defined (two consecutive failures: non-zero exit, >120s timeout, or auth error; flip recorded in the run journal).
- Open items: v4.1.9's 10 plus the smoke-added desktop add-repo picker defect stand; **THREE added by this patch** (critique bullets 6/7/10, recorded not fixed) — (a) the capture/ingest contract for CLI critique output into the Event Bus is undefined; (b) the Owner machine is a single point of failure for the Review gate while the CLI is the default path; (c) the new path has no independent validation yet — its first critique was self-referential. No push ordered this cycle: commit + annotated tag v4.1.10 + bundle only.

## v4.1.11 (2026-08-18, interconnect + critique policy + nightly hygiene)

**What changed**

- ADDED `## Critique policy (quality-preserving efficiency)` to BOTH package files in the same patch (L-20260817-04), placed after Credit-Aware Routing in each, substance verbatim rather than paraphrased (L-20260817-06): routine = 1 focused Grok Heavy pass; significant (system rules, routing, safety/hard stops, multi-file package changes, or anything an Owner order names significant) = the 3-pass ladder Pass 1 Defects / Pass 2 False-green / Pass 3 Final adversarial; every major Grok finding APPLIED or explicitly REJECTED with a one-line journaled reason; "LGTM" or an empty critique = FAIL on significant work; standing rule "Efficiency may reduce the FREQUENCY of expensive steps, never the DEPTH required for significant work."
- ADDED `docs/GROK.md` — how Grok plugs in: role (critique/advisor only, UNVERIFIED until a Claude gate verifies, no write path, permanent), what Grok reads (public Drive links preferred over pasted walls; small diffs only; never a GitHub link, the repo is private and Grok cannot open it), the critique ladder, the required output shape (blunt numbered bullets, one evidence pointer each, false-green risks, open questions), and four copy-verbatim prompt blocks (1 routine + 3 significant passes).
- ADDED `docs/HANDOFF-FORMAT.md` — the required end-of-run block (HANDOFF / SHA / Drive / Changed / Significant / Grok passes requested / Open items / Ask Grok), a filled example, and per-field rules: SHA is a pushed commit or exactly `STAGED (unpushed)` and never a claim; Significant decides 1 vs 3 Grok passes; Ask Grok is a concrete question.
- ADDED `docs/OWNER-QUICK-REFERENCE.md` — one page for the Owner: the two links, who does what (Fable / Opus / Grok / Cowork), how to start a run (Code tab → select `autonomy-system` AT CREATION → paste the order; Cowork is hands only), efficiency-with-quality, the nightly task, landing tiers, the HANDOFF block, hard stops.
- ADDED `docs/NIGHTLY-HYGIENE.md` + `docs/nightly-checklist.json` — unattended 15–20 minute docs-only hygiene pass, steps a–g (credit check → lessons + journal tail → specguard on both package docs → open-item owner/exit refresh → one surplus cleanup → nightly journal record → land per LANDING-PROTOCOL), with the credit rule (skip Grok when Claude is CLAUDE_CRITICAL unless specguard fails), hard stops unchanged, and explicit failure behaviour (record and continue; never silent-skip, never false-green). The JSON mirrors the same ids a–g with `required` / `on_fail` for machine use; `journal_record_type` = `nightly_hygiene`.
- ADDED `docs/run-journals/INTERCONNECT.md` — plain-language map of the loop: GitHub is the one true copy (Claude writes) → Actions mirrors `docs/` to Drive on every push → Drive is public-read so Grok reads the same files by link → Grok's bullets return as UNVERIFIED → a Claude gate applies or rejects with journaled reasons → landing per LANDING-PROTOCOL. Carries the HANDOFF block as the baton and closes with the deferred Phase-2 note.
- UPDATED `CLAUDE.md` (surgical, 70 lines): links pair (private GitHub canonical / public Drive mirror), Critique policy pointer, Cowork role line (hands + activator, not a git source of truth, no live-write authority), landing-tier pointer, nightly hygiene pointer, and the REQUIRED end-of-run HANDOFF block. Existing content preserved.
- UNCHANGED elsewhere: both package files are byte-identical outside the title line, the new section, and the history rows — except the SPEC header `**Date:**` which advanced 2026-08-17 → 2026-08-18 with the version bump (flagged at the gate, one-line revert if unwanted). No architecture change, no collapse rule, no Grok Bot activation, no new dependency.

**Why**

Owner directive of 2026-08-18: seamless interconnect, quality-preserving critique, nightly hygiene. The system already had a canonical repo, an Actions mirror, and a landing protocol, but nothing wrote down how Grok actually receives files (it had been getting pasted walls and links to a private repo it cannot open), nothing fixed how deep critique must go when efficiency pressure rises, and nothing kept the docs honest on days with no run. This patch closes the three gaps with documentation only.

**Free tools adopted now vs deferred**

- ADOPTED (already live, $0): private GitHub canonical + `sync-docs-to-drive.yml` Actions mirror + the Google Drive folder; the public anyone-with-link READER Drive links (verified 2026-08-18) as Grok's read surface; a Claude scheduled task (free) for the nightly hygiene run — fires 01:30 UTC daily, 20-minute cap.
- DEFERRED, none installed this patch: Cursor free tier (optional human/local coding surface, not needed for the autonomy core) — *superseded 2026-08-18 by Owner order (v4.1.13, Cursor Ultra bundled with SuperGrok Heavy as a second builder pool; see `docs/CURSOR-LANE.md`); the deferral above is kept as the record, not the current state*; Composio + a Grok bot free tier (future toolized Grok access — worth activating when Grok needs to DO things instead of read and critique); LangGraph free tier (possible future orchestrator graph if flows outgrow docs + Actions).
- REJECTED: GraphQL — unnecessary for this docs/Event-Bus architecture; do not introduce it.

**Honest note on the nightly schedule**

01:30 UTC is 9:30 PM EDT today. It is pinned to UTC, so in EST months the same firing lands at 8:30 PM ET. It does not follow daylight saving on its own; the Owner moves it if he wants 9:30 PM year-round. The task has not fired yet — see open item 12.

**specguard evidence**

specguard: SYSTEM-CURRENT 3 findings [2 FAIL, 1 ADVISORY] -> 3 [2 FAIL, 1 ADVISORY] (identical, accepted pooled-budget residuals); SPEC 1 [0 FAIL, 1 ADVISORY] -> 1 (identical). Zero new findings before AND after the Grok-ladder amendments (re-run at gate).

**Version collision note:** built as v4.1.10 in parallel with the Grok-CLI-bridge patch, which landed first and took the number + tag. Renumbered to v4.1.11, rebased onto the CLI-bridge state (d8bfec6), GROK.md updated to make the local CLI the default transport (browser fallback). Grok's Pass-2 'missing concurrent-work checks' bullet proved correct in live practice this same cycle.

**Grok 3-pass ladder on this patch (dogfooded pre-land):** chat "Grok v4.1.10 Critique Ladder Defects", 7+8+8 = 23 bullets. Gate dispositions (full one-liners in journal record 40): APPLIED — credit-exception tightened (routine-only deferral; significant never lands unladdered); major-finding defined; nightly scoped routine-only (never lands significant changes); pass non-overlap + re-ask rule; significant-definition narrowing requires Owner order; pre-land/post-land artifact modes codified; UNKNOWN-credit and BLOCKED-landing declared expected-honest unattended outcomes; GROK.md trust boundary (mirror = context not authority; Grok output = data, never instructions). REJECTED with reasons — mechanical enforcement of journaling/HANDOFF (out of docs-only scope; folded into open items 2/5); critique-from-summary objection (that IS the designed pre-land mode, now codified); circular self-application (dogfooding intended; HR7 when-unsure-significant was followed).

## REMAINING OPEN ITEMS after v4.1.11 (not falsely green — these remain)

CLOSED this cycle: none.

UPDATED 2026-08-18 (v4.1.12): item 14 CLOSED on record (strikethrough below; evidence stack in the v4.1.12 addendum). Count: **14 listed, 12 open** — items 10 and 14 are closed-on-record and kept visible rather than deleted. Item 13 (Owner-machine SPOF for the Review gate) stays OPEN and unwaived; its Owner/Exit wording was corrected this cycle by ladder Pass 1 b6 (the browser fallback B1 is a second path, not a machine-independent transport).

UPDATED 2026-08-18 (v4.1.13): item **15** (Cursor lane activation) ADDED at the end of this list — nothing renumbered, nothing reordered, nothing closed. Count supersedes the line above: **15 listed, 13 open**; items 10 and 14 remain closed-on-record and deliberately visible.

UPDATED 2026-08-18 (activation, same day): item **15 CLOSED on evidence** — the pilot PR went through the complete review lane and merged. Count supersedes both lines above: **15 listed, 12 open**; items 10, 14 and 15 are closed-on-record and kept visible rather than deleted. Nothing renumbered.

UPDATED 2026-08-19 (v4.1.14): item **16** (Autopilot lane) ADDED at the end of this list — nothing renumbered, nothing reordered, nothing closed. Count supersedes every line above: **16 listed, 13 open**; items 10, 14 and 15 remain closed-on-record and deliberately visible.

UPDATED 2026-08-19 (gate ratification, same day): item **16 CLOSED on evidence** — the first autopilot run was gated and RATIFIED (`gate_ratification` record 62, verdict PASS, zero overturns). Count supersedes every line above: **16 listed, 12 open**; items 10, 14, 15 and 16 are closed-on-record and kept visible rather than deleted. Nothing renumbered.

UPDATED 2026-08-19 (v4.1.15): item **17** (Owner routing directive + mandatory usage receipts) ADDED at the end of this list — nothing renumbered, nothing reordered, nothing closed. Count supersedes every line above: **17 listed, 13 open**; items 10, 14, 15 and 16 remain closed-on-record and deliberately visible.

UPDATED 2026-08-19 (gate ratification, same day): item **17 CLOSED on evidence** — the v4.1.15 surgery was gated and RATIFIED (`gate_ratification` record 67, verdict PASS, zero overturns, zero fix loops), and the first gate-composed USAGE RECEIPT was delivered with it, embedded in `docs/LATEST-HANDOFF.md` by the closing commit. Count supersedes every line above: **17 listed, 12 open**; items 10, 14, 15, 16 and 17 are closed-on-record and kept visible rather than deleted. Nothing renumbered.

UPDATED 2026-08-19 (v4.1.16): item **18** (Speed pack + lane auto-briefings) ADDED at the end of this list — nothing renumbered, nothing reordered, nothing closed. Count supersedes every line above: **18 listed, 13 open**; items 10, 14, 15, 16 and 17 remain closed-on-record and deliberately visible.

UPDATED 2026-08-19 (v4.1.16 extension, LIVING SYSTEM MAP): item **18 EXTENDED, NOT CLOSED** — `docs/SYSTEM-MAP.html` and `tools/gen_map.py` join the regeneration law as its fourth generated output, C6 now checks the map through the same generalised checker, and this run journaled both exit conditions (pack-first self-brief; lean-scribe compensating control for `e009fd3`). NOTHING renumbered, reordered, added or closed: the count is UNCHANGED at **18 listed, 13 open**, and item 18 stays OPEN until the gate closes it on that evidence.

UPDATED 2026-08-19 (gate ratification, same day): item **18 CLOSED on evidence** — the LIVING SYSTEM MAP run was gated and RATIFIED (`gate_ratification` record 79: `verdict` exactly `PASS`, `overturns` **0**, `fable_phases` **2** so 2 − 2 = **zero fix loops**), and item 18's exit was checked field by field against the journal rather than against the crew's report. Count supersedes every line above: **18 listed, 12 open**; items 10, 14, 15, 16, 17 and 18 are closed-on-record and kept visible rather than deleted. Nothing renumbered, nothing reordered, nothing added.

UPDATED 2026-08-19 (Cursor harvest procedure): item **19** (Cursor harvest procedure — pounce + batch) ADDED at the end of this list — nothing renumbered, nothing reordered, nothing closed. Count supersedes every line above: **19 listed, 13 open**; items 10, 14, 15, 16, 17 and 18 remain closed-on-record and deliberately visible.

1. **Pooled Fable/Opus budget** — two separately-budgeted models still report as one figure in the CREDIT-CHECK template and the shared threshold table; "Fable exhausted, Opus fine" is inexpressible and a note written from the template cannot pass `specguard --check-note`. Duplicated at a second site (SPEC) since v4.1.9 and unlinted there. *Owner:* Chris (decision on split reporting). *Exit:* both package files carry per-model budget lines and a note written from the template passes `specguard --check-note`.
2. **specguard cross-file version-sync check** — still proposed, not built; must compare section presence and content across package files, not just version rows (L-20260817-06). *Owner:* Claude (Opus tools unit, build in Phase A/C of the automation roadmap). *Exit:* specguard flags a version row or body section present in one package file and missing in its sibling, proven against the v4.1.9 drift as a regression fixture.
3. **SPEC team table (L48–52)** still uses pre-split phrasing ("Claude Code (Opus/Fable)", "Claude Code + Opus 5"), inconsistent with the rebound Hard Rule 1 and routing table. *Owner:* Claude (Opus unit, next SPEC-touching patch). *Exit:* no pre-split phrasing remains in the SPEC team table.
4. **v4.1.5 condensation drops** remain unrestored in FOR-CLAUDE (UNVERIFIED→verified promotion path owner, "raise an Issue" on 3 fails, "never an authority", non-significant carve-out, worktrees/Grok-Build bridge as shared infra). *Owner:* Claude (Opus unit) proposes, Fable gates. *Exit:* each of the five is either restored in the package or recorded as an intentional drop with a reason.
5. **Write-safety auditability** — Hard Rules 1/2/4 remain honor-system: no write-ledger, no actor/model attribution on writes (an audit log of system writes; unrelated to the money/inventory ledgers that hard stops forbid). *Owner:* Chris authorizes scope, Claude builds. *Exit:* every landed write carries actor + model + timestamp in a ledger file that a run can reconcile against.
6. **Self-test pass criteria and the 3-fail counter scope** remain undefined in both docs. The v4.1.10 Critique policy partially addresses this — it defines critique depth and makes an empty critique a FAIL on significant work — but it does not define what counts as a self-test pass, nor what increments or resets the 3-fail counter. Kept open for exactly that. *Owner:* Claude (Fable) drafts, Chris ratifies. *Exit:* both package files define a self-test pass and the counter's scope and reset condition.
7. **Grok "low" is unquantified**, so the CONSERVE_BOTH trigger stays a judgement call; the reset moment is transcribed from the Owner directive, not verified against the provider UI, and the SPEC mirror copies the imprecision unchanged. *Owner:* Chris (one browser read of the xAI usage page). *Exit:* a numeric or plan-level definition of Grok "low" recorded in Credit-Aware Routing, sourced from the provider UI.
8. **Heuristic advisories** — 'Hierarchy' (FOR-CLAUDE) and 'Layers' (SPEC) unattributed in history; keyword-heuristic noise, listed for completeness. *Owner:* Claude (Opus, cosmetic). *Exit:* either a history row references them or specguard's heuristic gains an allowlist — closing as WONTFIX with a recorded reason is acceptable.
9. **Deferred by directive, unchanged** — Event Bus duplicate-collapse rule (Phase 2, no rule invented here); Grok Bot Layer (deferred, not cancelled, not activated here). *Owner:* Chris. *Exit:* an explicit Owner order activating Phase 2, or an explicit cancellation.
10. ~~Actions sync BLOCKED_ON_OWNER_SECRETS~~ — CLOSED 2026-08-17: sync-docs-to-drive is LIVE on Owner OAuth with a Drive-visible create proof, service-account path retained as update-only fallback. Kept on the record so the closure is visible rather than silently dropped. *Owner:* n/a. *Exit:* met.
11. **Desktop add-repo-to-existing-session picker defect** — the app cannot attach a repo mid-session (OAuth registration errors, refs ofid_c89a3ea091e63676 / ofid_6905a097c17ba275, then "Connection to server failed" ×3, 2026-08-17 evening, app 1.32352.0; status page green throughout — app-side, not platform). Workaround: attach the repo at session creation, or land via Tier 2. *Owner:* Chris (report to Anthropic support with both ofid refs). *Exit:* a repo is successfully added to an EXISTING session.
12. **NEW — nightly task observability** — the 01:30 UTC hygiene task is created but has never fired; its behaviour, its runtime against the 20-minute cap, and its landing tier are all unobserved. Nothing about it may be reported as working until a run exists. *Owner:* Chris confirms the scheduled task is enabled; Claude reads the result on the next run. *Exit:* the first green `nightly_hygiene` record in `docs/run-journals/run-journal.jsonl`, mirrored to Drive.
13. **NEW — Owner-machine single point of failure for the Review gate** (from the v4.1.10 gap (b)): while the local Grok CLI is the default critique path, no critique can run when the Owner desktop is off, so the Review gate has one physical dependency. *Owner:* Chris decides how the SPOF is handled. Honest candidate order, corrected 2026-08-18 by the v4.1.12 ladder Pass 1 (bullet 6): the EXISTING free browser fallback (roadmap B1) is a second PATH, not a second MACHINE — SYSTEM-MAP B1 itself says it "needs Owner's Chrome online", so exercising it as an independent scheduled path mitigates CLI failure while the desktop is up and does NOT remove the single point of failure. Only a headless transport does (roadmap B3/C1, the xAI API bridge), and that stays Owner-escalated: a key means credentials and spend (Hard Rule 6). *Exit:* EITHER a HEADLESS second transport exists and has been exercised once, OR the Owner signs off accepted-risk in a journaled line whose re-open is evaluated RETROACTIVELY by the next gate session (v4.1.12 ladder Pass 2 b6): nothing watches while the machine is off, so if significant work sat waiting for critique during any offline period, the waiver is void ab initio for that period, this item re-opens, and the re-open is journaled at next contact — no live watcher exists or is claimed. A B1 run may be recorded here as partial mitigation; on its own it does not meet this exit.
14. ~~CLI critique path independent validation~~ — **CLOSED 2026-08-18 (v4.1.12)**, on evidence, not on assertion: the 3-pass ladder ran through the default CLI path on the optimize target (journal records 43/44/45 — 8+8+8 = 24 bullets, 13 applied, 11 rejected, every bullet dispositioned, no empty pass, `transport` `cli` all three, 210s/344s/294s); the work landed as **c8d8884**; the Drive mirror was CONTENT-verified 2026-08-18 (the new `## Critique journal contract` heading present in the mirrored GROK.md, not merely an advanced `modifiedTime`); and one further post-land pass-0 critique ran on the LANDED Drive text itself (cli, exit 0, 185s, 6 bullets — journal record 46), reviewing published text this cycle did not write. CAVEAT, recorded not waived — Pass 3 reject-b5, verbatim from record 45: "Owner order defined non-self-referential as not-the-CLI-bridge-text and this target is open-items governance prose; PRESERVED VERBATIM for the next gate - Grok argues the target is still same-system governance prose written by the same orchestration, so a truly independent target (external artifact or third-party review) would be stronger evidence before item 14 closes; closure stays a next-patch decision". The Owner order counts this target as non-self-referential and closure is taken now; the independence objection is NOT retired — the next significant ladder must use a non-governance / external target to retire that objection class. Scope of this closure (v4.1.12 ladder Pass 3 b1/b5): closure of item 14 validates critique QUALITY on the default path only; Review AVAILABILITY remains open as item 13 — the Review gate is NOT autonomy-ready until item 13 resolves (headless transport or Owner decision). *Owner:* n/a (closed). *Exit:* met — full evidence stack and the five verbatim Pass-3 rejections are in the v4.1.12 addendum below.
15. ~~Cursor lane activation~~ — **CLOSED 2026-08-18**, on evidence, not on assertion: the pilot ran the FULL review lane and merged. Evidence stack: Cursor agent `bc-88581fe2-6b2c-4b2c-b6d0-eae91640652a` opened **PR #2** (branch `cursor/pilot-self-introduction-652a`, exactly `docs/CURSOR-PILOT.md` +25 — in scope, and carrying its own retrieval_ref `LM-RET-2026-08-18T23:20Z-A`); **`verify-docs` ran on the pull request and SUCCEEDED — Actions run 32196858563**, the first CI conclusion this lane has ever produced, courier-witnessed and independently cited by Grok; a Grok batched routine critique of both open PRs (cli, exit 0, 150s, 6 bullets, all six applied — journal record 55); and a **Claude gate merge, commit 5ce249f on `origin/main`**, which is also the proof that the ruleset's admin bypass works. Dispatch records ride the closing commit: record 56 (`outcome` `merged`) and record 57 (`outcome` `closed_out_of_scope`). Access state at closure: the Cursor app is scoped from ALL repositories down to `autonomy-system` only, and ruleset **protect-main-cursor-lane** is Active (main requires pull requests; bypass = Repository admin + Claude app, always-allow; deletion and force-push toggles deliberately left off). SCOPE DISCIPLINE, recorded because it is the more useful evidence: **PR #1 was CLOSED, not merged** — it touched `.cursor/environment.json`, outside the `docs/**` + `tools/**` dispatch scope, and had zero completed checks (the exact green-by-absence case). Its four lines were re-landed CLAUDE-AUTHORED in the closing commit with Grok's b6 hardening (no `--upgrade`). The lane's first live decision enforced the rules rather than making an exception for itself. *Owner:* n/a (closed). *Exit:* met — and what remains open is measurement, not activation: Ultra's usage limits are still UNKNOWN (the pilot ran on Auto), and one pilot is not a track record.

16. ~~Autopilot lane (one-paste bookends with gate fix-loop)~~ — **CLOSED 2026-08-19**, on evidence, not on assertion: the first autopilot run was gated and RATIFIED. Evidence stack: `gate_ratification` **record 62** in `docs/run-journals/run-journal.jsonl`, which meets this item's exit test field by field with no judgement call — `verdict` exactly `PASS`, `target` beginning with the literal prefix `autopilot: `, `overturns` **0**, and `fable_phases` **2**, so 2 − 2 = **zero fix loops** ran; the cited landed commit **3ce0fc3** (tag `v4.1.14`, parent `59aaa51`), which the gate read from the live clone rather than from the crew's report; and the run's three `grok_critique` records **58, 59 and 60** (passes 1–3, 24 bullets, 21 applied, 3 rejected on journaled reasons), with run record **61** verified against the landed content. The gate read all 24 bullets in full and overturned none. HONEST LIMIT, recorded not waived: this closes ACTIVATION — one clean pass through the lane — and NOT the lane's failure paths. A `FIX` verdict has never been issued, the 3-loop ceiling has never been hit, a `BLOCK` to the Owner has never been exercised, and `fable_phases` remains an unmetered proxy (open items 2 and 5); one run is not a track record. Record 62 also carries three exceptions on the record: the pre-existing `--strict` failure on legacy item 10 (accepted, not reversed here), the crew's **estimated** `ts` fields on records 58–61 — two of which postdate the run's actual finish; hygiene defect, lesson `L-20260819-01` — and two over-length Grok prompt files accepted under the `PROMPT_CHARS` compensating control. *Owner:* n/a (closed). *Exit:* met.

17. ~~Owner routing directive + mandatory usage receipts~~ — **CLOSED 2026-08-19 (gate ratification, same day)**, on evidence, not on assertion: the directive is on `main` and IDENTICAL in both package files, and the first gate-composed usage receipt has now actually been delivered. Evidence stack: `gate_ratification` **record 67** in `docs/run-journals/run-journal.jsonl` — `verdict` exactly `PASS`, `overturns` **0**, `fable_phases` **2** so 2 − 2 = **zero fix loops**, with the ladder-added SCOPE paragraph read in full by the gate and **ADOPTED as ratified policy text** (routing preference only, no authority rule amended, hot-path and surgery exclusions accepted as written); the landed commit **e0965356c05d0861214150f2f6b0d10ad3a044a6** (tag `v4.1.15`, parent `3af3ca9`), which the gate read from the live clone rather than from the crew's report, the landed directive text confirmed verbatim against the Fable-authored order; the run's three `grok_critique` records **63, 64 and 65** (passes 1–3, 24 bullets, 24 applied, 0 rejected) — all 24 bullets read in full by the gate, so the zero-rejection ladder was AUDITED rather than waved through, and the two partial-applies are transparently recorded inside their APPLY reasons (record 64 b8's "credit saved" field deliberately not built; record 65 b4's premise correction written into the text) — with run record **66** verified against the landed content; and the **USAGE RECEIPT embedded in `docs/LATEST-HANDOFF.md` by this closing commit**, the first ever delivered, whose Grok-pass line (3 passes, 84/99/124s, records 63–65) and Cursor line (not dispatched, routing note journaled, record 66) reconcile against this run's journal records. NOT CEREMONIAL, which this item's own exit demands: the closing run SPAWNED a crew agent (`a106e9cb`, 240603 tokens as self-reported by the spawn result), so the receipt carries per-agent lines rather than a column of zeros, and it carries the ROUTING LINE — Opus authoring and landing, Grok critique, Cursor excluded as surgery-class, the first exercise of the new exclusion. INSTALLED at v4.1.15 — the Owner's standing order of 2026-08-19, written into both package files: Claude usage is the scarce resource and is conserved first; above-trivial repo-based coding routes to the Cursor lane BY DEFAULT; Grok capacity is used liberally for critique, drafting, research and large-text summarization as UNVERIFIED input to a Claude gate; Fable appears only at bookends and gates; and EVERY run ends with a gate-composed USAGE RECEIPT (tokens per spawned agent as reported by the spawn results, `fable_phases`, Grok passes with durations, Cursor dispatches). WRITTEN, and now EXERCISED ONCE — the first receipt was delivered by the closing gate (record 67); still nothing mechanically checks that a receipt exists or that its counts are true (that enforcement is items 2 and 5, the same gap `fable_phases` sits in). Honest limits recorded not waived: the Anthropic plan meter is NOT machine-readable, so a receipt is a PROXY for spend and the Owner still reads the true meter in the Claude app's usage settings; token counts are self-reported by the same orchestration that composes the receipt, so a receipt is bookkeeping evidence, not an audited bill; and "above trivial size" is a gate judgement call with no definition here, so the Cursor-first default can be honoured or evaded by the same word. SCOPE OF THIS ITEM, stated so its closure cannot be over-read: the exit below closes INSTALLATION — the directive landed and one receipt was actually produced — and NOT compliance. It cannot prove that a later run honoured the routing split, that a receipt's token numbers were true, or that a boilerplate non-dispatch reason was ever rejected; ongoing compliance is gate discipline until items 2 and 5 mechanise it, and the honest reconciliation available today is that Grok passes, Cursor dispatches and `fable_phases` must match the journal records while the token line matches nothing. *Owner:* n/a (closed) — Chris still reads the receipts and says whether they answer the question he actually asked; Claude composes them at the gate. *Exit:* **met**, field by field: the directive present and IDENTICAL in both package files on `main` at `e096535` (sha256 of the added block matching across the two, `980cd8d9…` for the directive and `689c9fb7…` for the full added region including the SCOPE paragraph); a gate-composed usage receipt DELIVERED on a ratified run and cited by its `gate_ratification` record number (**67**); its Grok-pass and Cursor lines reconciling against that run's journal records (63–66); the closing run non-ceremonial, having spawned crew agent `a106e9cb`; and the routing line present with the non-dispatch reason recorded (surgery-class exclusion, not a boilerplate). What is NOT met — because it was never in this test — is compliance on any later run.

18. ~~Speed pack + lane auto-briefings (v4.1.16), extended by the LIVING SYSTEM MAP~~ — **CLOSED 2026-08-19 (gate ratification, same day)**, on evidence, not on assertion: the briefing backstop is INSTALLED on `main` and has now been EXERCISED ONCE end to end by a run that was not the run that built it. Evidence stack, every element a repo artifact the gate read for itself: `gate_ratification` **record 79** in `docs/run-journals/run-journal.jsonl` — `verdict` exactly `PASS`, `overturns` **0**, `fable_phases` **2** so **zero fix loops**; the landed commits **f13b1e5f26fa58e825795ac0bb2743962469cd6d** (the map run) and **a1097a8** + **f17aa1a** (the speed pack and its correction, ratified earlier at record 74); the map run's ladder records **75, 76 and 77** — all 24 bullets read in full by the gate, both rejections AUDITED and UPHELD (75 b6, the CI wiring, out of that run's ordered scope and already pre-authorized at record 74; 77 b1, a stale-paste artifact, verified against the live file rather than argued) — with run record **78** verified against the landed content; and the gate's own Drive read at 15:40Z, taken EARLY at the gate's discretion rather than deferred, which found the mirrored `docs/SYSTEM-MAP.html` PRESENT with `mimeType` `text/html` (so the `tools/drive_sync.py` `.html` MIME row is PROVEN, not unproven), title "Living system map — autonomy-system v4.1.16", ~34k. EXIT MET FIELD BY FIELD: the generator and the staleness check live on `main` with all four generated files present and FRESH (`--all` exit 0, no `brief-pack-stale` and no `system-map-stale` finding); the first SUBSEQUENT run self-briefed via `docs/BRIEF-PACK.md` and journaled that self-brief WITH an honest sufficiency verdict rather than a green tick (record 78, which names what the pack did not carry); the lean-scribe compensating control for `e009fd3` was discharged by CONTENT, not by timestamp (record 78: the mirrored `docs/LATEST-HANDOFF.md`, Drive id `1kTmQYp3_D2ICLnocaY_SD0esgAOXXzym`, CONTAINS `record 74`); and this run's Owner receipt carries the ROUTING LINE (Owner elected the Opus fast lane; reason recorded as OWNER ORDER and SPEED; the Cursor-first default is unchanged and was not quietly re-written by the exception). SCOPE OF THIS CLOSURE, stated so it cannot be over-read: this closes **INSTALLATION and FIRST EXERCISE** — the machinery exists, it ran once from end to end, and a later run actually used it — and NOT ongoing compliance, not pack QUALITY, and not the residuals below, which stand as STANDING LIMITS on a closed item rather than being retired with it. WHAT THE ITEM RECORDED WHEN IT WAS OPENED, kept verbatim on the board rather than deleted: `tools/gen_brief.py` generates three briefings from the canonical documents (`docs/BRIEF-PACK.md`, `docs/GROK-CONTEXT.txt`, repo-root `AGENTS.md`), `tools/validate_journal.py --all` fails a stale pack, and the rule set that hangs off them (self-brief via pack, the regeneration rule, the GROK CONTEXT rule, lean scribe, batch gating, parallel-crew fan-out) is WRITTEN but only partly EXERCISED: the generator ran and the staleness check fired in this cycle, but no LATER run has yet self-briefed from the pack, and the lean-scribe deferral has never had its compensating control journaled by a following run. What this item does NOT claim: that a fresh pack means the pack is GOOD (freshness proves only that the sources have not moved), that deleting the pack would be caught (an absent pack skips the check cleanly), that Cursor agents actually read `AGENTS.md` (unobservable from here; the dispatch template names it as belt-and-braces), or that anything mechanically forces a batch gate to write one record per run. NAMED RESIDUALS from the v4.1.16 ladder, recorded rather than quietly carried: (i) **deleting the generated files is still a kill switch** for the whole briefing backstop, because an absent pack skips cleanly by the Owner-ordered semantic — the detector is `python3 tools/gen_brief.py --check` in the pre-land step, and wiring it into `.github/workflows/verify.yml` would close it mechanically but `.github/**` was outside this cycle's scope; (ii) **batch eligibility and the `Significant` field are unmechanised**, so a split-run evasion (several runs that together change one control, each labelled routine) is caught by gate judgement alone; (iii) **whether an outside agent reads or re-reads `AGENTS.md`** is unobservable from this repo, including the stale-cache case on a re-used Cursor agent; (iv) **`docs/BRIEF-PACK.md` is ~530 lines against the ~350-line target**, a deliberate cost of ladder Pass 2 b1 (the pack now carries the SPEED PACK rules verbatim rather than pointing at them) — the generator prints the overage as a NOTE rather than hiding it, and trimming is a later call for the gate, not a silent one. *Owner:* Claude — the next run of any shape exercises it; Chris only if the pack proves not worth reading. *Exit:* the generator and the staleness check live on `main` with all three generated files present and FRESH (validator `--all` exit 0 with no `brief-pack-stale` finding), AND the first subsequent run self-briefs via `docs/BRIEF-PACK.md` and journals both that self-brief and the lean-scribe compensating control (the deferred Drive CONTENT re-read of the v4.1.16 landing) on its run record. **EXTENDED 2026-08-19 (SIGNIFICANT, non-surgery, no version bump): a FOURTH generated output joins the regeneration law.** `tools/gen_map.py` (NEW) generates `docs/SYSTEM-MAP.html`, the Owner-facing LIVING SYSTEM MAP — one self-contained page, no network, no stored state, five panels in lay language (team and meters, the one-paste loop, the gauntlet, the self-briefing loop, current state). Every figure on it is INJECTED from a canonical source at generation time (version, open/closed counts, critique pass counts and pass names, the fix-loop bound, the three landing tiers, the four Owner lanes, the last landed SHA and landing summary, the validator self-test count read from a new self-verifying `SELF_TEST_CASE_COUNT` constant, the journal record count) and every LAY CLAIM is ANCHORED to an exact phrase that must still exist in a named source — a missing phrase is a loud exit 2, and the whole anchor table is PUBLISHED on the page so a reviewer can check the one thing no machine here can. `docs/EFFICIENCY-MODE.md` SPEED PACK now requires BOTH generators and all four outputs in the same commit, names `system-map-stale`, extends loopholes (i) and (ii), and renames the fan-out carve-out from the GENERATED TRIO to the GENERATED SET with an explicit no-hand-merge rule for generated files. `tools/validate_journal.py` C6 was GENERALISED, not forked: one `check_generated_doc` now serves both outputs and `check_brief_pack` / `check_system_map` are thin wrappers; self-test 18 to 26. `docs/RUN-TEMPLATE.md` gains `gen_map.py --check` in the pre-land step, the map-last ordering, and a GATE duty to spot-check three anchor rows against their paragraphs. **CREW-SIDE EVIDENCE FOR THIS ITEM'S EXIT, journaled not asserted:** (a) this run performed the first PACK-FIRST SELF-BRIEF — `docs/BRIEF-PACK.md` read first, freshness proven by `validate_journal.py --all` exit 0 at base `e009fd3`, with the canonical files it still had to open named on the run record; (b) this run performed the LEAN-SCRIBE COMPENSATING CONTROL for `e009fd3` — the mirrored `docs/LATEST-HANDOFF.md` (Drive id `1kTmQYp3_D2ICLnocaY_SD0esgAOXXzym`) was read and CONTAINS the string `record 74`, so the deferred Drive CONTENT check was discharged rather than aged out. The crew did NOT self-close on that evidence — it supplied the evidence and the gate closed the item at record 79, which is the division of labour this item's own exit demanded. **NEW RESIDUALS from this ladder, recorded rather than carried quietly:** (v) an anchor proves a phrase is PRESENT, never that the paragraph around it still MEANS the claim — the only detector is the gate's anchor spot-check, which is human discipline; (vi) the map's journal record count is a build-time FLOOR that LAGS after a journal append without ever going STALE, so `gen_map.py --check` and the map-last ordering are the only detectors and both are builder-side; (vii) CI still runs neither `--check`, so a DELETED map keeps `--all` at exit 0 — the gate pre-authorized the `.github/**` wiring at record 74 and it stayed out of this run's ordered scope; (viii) one crew wrote the generator, the checker extension and the page the checker guards, so the pass condition is partly circular — mitigated by the published anchor table and the three-pass ladder, not removed. **STANDING LIMITS AFTER CLOSURE, carried on the closed item rather than retired with it:** residuals **(v)** an anchor proves a phrase is PRESENT, never that the paragraph around it still MEANS the claim, **(vi)** the map's journal record count is a build-time FLOOR that LAGS after a journal append without ever going STALE, **(vii)** CI still runs neither `--check`, so a DELETED generated file keeps `--all` at exit 0, and **(viii)** one crew wrote the generator, the checker extension and the page the checker guards, all remain TRUE and RECORDED as of this closure; so do the earlier residuals (i)–(iv). Closing the item closes the exit test, not these limits, and none of them is renumbered into a new item — a later run that wants one mechanised opens it as a new numbered item and says so. *Owner:* n/a (closed) — the `.github/**` `--check` wiring pre-authorized at record 74 and re-affirmed here is the standing next step whenever a run touches that path. *Exit:* **met** — generator, both checkers and all four generated outputs present and FRESH on `main`; a subsequent run self-briefed from the pack and journaled the self-brief with an honest sufficiency verdict (record 78); the lean-scribe compensating control discharged by CONTENT for `e009fd3` (record 78); ratified at `gate_ratification` record 79, PASS, 0 overturns.

    GATE NOTE 2026-08-19 — **RATIFIED by `gate_ratification` record 74** (verdict PASS, 0 overturns, both commits: a1097a8 tag v4.1.16 and the correction f17aa1a); the item stays **OPEN**, because its exit needs the NEXT run's self-brief and the lean-scribe compensating control, which no ratification can supply. Gate rulings recorded here: wiring `python3 tools/gen_brief.py --check` into `.github/workflows/verify.yml` is AUTHORIZED for the next run that touches `.github/**` (closes residual (i) mechanically; absent-pack semantics stay clean-skip plus an explicit validator advisory until then); and the frozen 11-to-17 history rows in the tagged package files are ACCEPTED as a recorded inaccuracy, with the correction riding the next package-file ladder rather than a retargeted tag.

    GATE NOTE 2026-08-19, SUPERSEDING THE LINE ABOVE — **RATIFIED by `gate_ratification` record 79** (verdict PASS, 0 overturns, `fable_phases` 2 so zero fix loops, commit f13b1e5f26fa58e825795ac0bb2743962469cd6d): the "stays OPEN" sentence in the record-74 note above is now SPENT, because the thing it was waiting for happened — the NEXT run self-briefed from the pack and journaled the lean-scribe compensating control (record 78), so the item is **CLOSED** as written on the item line. Two rulings carried forward unchanged: the `.github/**` `--check` wiring stays AUTHORIZED for the next run that touches that path, and the frozen 11-to-17 package history rows stay ACCEPTED as a recorded inaccuracy. One ruling ADDED: the gate performed this run's deferred Drive CONTENT check EARLY rather than deferring it — the mirrored `docs/SYSTEM-MAP.html` is present with `mimeType` `text/html`, which PROVES the `tools/drive_sync.py` `.html` row that landed UNPROVEN at f13b1e5 — and the two remaining named strings (`docs/SYSTEM-MAP.html`/"fourth" and `system-map-stale`, both in the mirrored `docs/EFFICIENCY-MODE.md`) stay DEFERRED to the next run under lean scribe, alongside this landing's own string `record 79`.

19. **NEW — Cursor harvest procedure (pounce + batch)** — WRITTEN 2026-08-19 (SIGNIFICANT, non-surgery, no version bump, no package files, no authority or routing text changed), NOT YET EXERCISED. `docs/CURSOR-LANE.md` gains **Harvest procedure (pipelined lane)**, the lane's timing rules, and the ownership is exact: the procedure lives there, and `docs/EFFICIENCY-MODE.md` and `docs/RUN-TEMPLATE.md` only POINT at it rather than restating it. What it installs: **(a) DISPATCH EARLY** — any wave containing Cursor work sends the dispatch BEFORE the Claude crews start building, so Cursor build time overlaps Claude work instead of following it, with the two failure modes named as the planner's problem rather than the reviewer's discovery (the agent is briefed on the PRE-WAVE `AGENTS.md`/`docs/BRIEF-PACK.md` by construction, so only wave-independent work is dispatched early and anything else is carried inline; and the base can move under an open PR, so CI green counts only on the CURRENT base and the gate re-runs it when the branch has fallen behind); **(b) POUNCE** — the responsible unit polls the PR from the moment of dispatch and starts the review the instant CI goes green, Grok diff critique immediately and evidence staged for the gate, with the polling unit NAMED at dispatch and a run that ends holding a green unharvested PR required to NAME it in the baton so the next run pounces; **(c) BATCH HARVEST** — several ready PRs may be reviewed and merged in one gate sitting, but each keeps its OWN `cursor_dispatch` record and outcome and its OWN diff critique at its own depth (records and critiques never merge, mirroring BATCH GATING's one-record-per-run rule), scope is judged per PR, and an out-of-scope PR is still CLOSED UNMERGED exactly as PR #1 was; **(d) THE INSPECTION FLOOR IS NON-COMPRESSIBLE** — `verify-docs` CI green + a Grok diff critique + a Claude gate merge, for every PR, at every batch size, under every credit state and against every deadline, restated from the routing directive's own scope paragraph in both package files, so pipelining removes WAITING and never CHECKING; **(e) a TIMING EXPECTATION on the record** — pipelined lane cost is roughly Cursor build time plus a few minutes of inspection, with Cursor build time explicitly outside this system's control and therefore never a commitment and never a reason to cut anything. SUPERSEDED PRECEDENT, named rather than quietly dropped: the pilot took ONE batched routine critique across BOTH open pull requests (journal record 55); (c) tightens that to one critique per PR. HONEST LIMITS, recorded not waived, because this item claims nothing it has not done: the procedure is WRITTEN and has never been RUN — no PR has yet been dispatched early, pounced on, or batch-harvested under it; **nothing polls on its own** (no watcher, no webhook, no scheduled check), so (b) is discipline carried by the baton and it fails exactly the way the lean-scribe deferral fails — a run that never happens never harvests anything; nothing mechanically enforces one critique per PR or one dispatch record per PR any more than it enforces batch-gating's one record per run (the same gap as open items 2 and 5); the timing claim in (e) is an EXPECTATION and not a measurement, and Cursor Ultra's usage limits remain UNMEASURED (open on item 15's closure note), so a queue or cap on Cursor's side makes build time arbitrary and nothing here would observe it; and the base-moved check in (a) is a gate reading, not a mechanical gate condition — as are the HARVEST-READY test, the current-base requirement, the re-critique rule and the batch-eligibility test, so all four of this procedure's defences are gate discipline and none is mechanical. **THE PRICE, from the ladder's Pass 3 and recorded rather than sold as free:** the lane's latency was never a control but FUNCTIONED as one, throttling volume in a lane whose review culture has never once rejected a PR on QUALITY (one merged pilot, one scope rejection, zero quality rejections) — removing the latency therefore delivers materially more Cursor commits to `main` with no new quality brake, on top of a Cursor-first default whose ties already break toward Cursor; the exact bad-merge sequence this enables is written into `docs/CURSOR-LANE.md` so a gate can watch for it; `verify-docs` is a docs/tools pack check and NOT an integration check, so early dispatch maximises tip/base divergence while the only integration judgement is human and at harvest time; harvest-last concentrates every Cursor merge decision onto the busiest turn of the run, which is why the gate is given an explicit right to SPLIT the harvest into its own sitting; part (d) is a RESTATEMENT and an anchor, never a control, so quoting it is not evidence that the floor was honoured; and this procedure FORBIDS the lane's only practiced critique shape (the record-55 batched critique) while replacing it with discipline nothing checks. Forbidding the practiced thing, installing the unexercised thing, then scaling volume on top is the risk ordering, recorded here rather than discovered later. *Owner:* Claude — the next wave that dispatches Cursor work runs it; Chris only if the lane's latency is still the reason work skips it, or if he wants volume held until a quality rejection exists. *Exit:* the FIRST REAL PR harvested under this procedure, closing on ORDERED ARTIFACTS rather than on narrative — a run report that says it pounced is not evidence. Required, in this order and each citable: (1) a `cursor_dispatch` record whose `ts` PREDATES the start of that wave's Claude build; (2) the `verify-docs` Actions run id for the merged tip, green on the base the merge actually used; (3) a `grok_critique` record for THAT diff, cited by record number, covering that PR alone; (4) the Claude gate merge SHA; with (1) < (3) < (4) in time on the record. The PR must be REAL WORK — a task dispatched because the wave needed it, not a self-introduction written to exercise the procedure — and if the sitting harvested more than one PR, each carries its own (1)(2)(3)(4). NOT IN THIS EXIT, and therefore not closed by it: that the lane can reject a PR on quality, that pipelined volume is safe at scale, and that any of the four gate readings above will actually be performed when a run is behind schedule.

## Optimize cycle — 2026-08-18

retrieval_ref `LM-RET-2026-08-18T11:38Z-E` (orchestration-memory@e1e7a5f51bab, repo@0a9a958; CLAUDE.md + GROK.md + HANDOFF-FORMAT.md + open items + lessons L-20260817-01…06 + journal records 42–43) — Fable gated, Opus executed. Docs/tooling only: no version bump, no package-file edit, no renumbering or reordering of open items.

- ADDED `## Critique journal contract` to `docs/GROK.md`: every Grok critique call — CLI or browser, routine or ladder — appends exactly ONE `grok_critique` record to `docs/run-journals/run-journal.jsonl` in the same commit as any applied fixes, with the full field list, one example line, and the three rules (silence on a major bullet is not a decision; an empty/LGTM critique is recorded with `bullets_count` 0 and status FAIL and does not count as a pass; a browser-fallback flip sets `transport` `"browser"`).
- CLOSED, narrowly — the part of v4.1.10 open item (a) that this commit actually settles is the **critique journal capture contract** (the `docs/GROK.md` section above; the schema text exists in this pre-land commit). NOT closed and explicitly still open: the roadmap ingest path — SYSTEM-MAP C1/G2, xAI key + `tools/grok_bridge.py` + `grok-critique.yml` writing Grok bullets into a `grok-inbox/` PR — which remains unbuilt; and enforcement, which is gate discipline here, not a mechanical check (that belongs to open items 2 and 5).
- NOW NUMBERED — v4.1.10 gaps **(b)** Owner-machine SPOF for the Review gate and **(c)** no independent validation of the CLI path are added to the authoritative v4.1.11 list as **items 13 and 14**, each with Owner + Exit (appended at the end; nothing renumbered). They were addendum-only prose, invisible to the machinery that reads the numbered list — nightly hygiene step d and the HANDOFF open-items count. Item 14 is expected to close at this cycle's landing verification and is deliberately NOT pre-closed.
- POLISHED in place — the v4.1.9-era REMAINING OPEN ITEMS 2 (specguard cross-file check), 5 (write-safety auditability) and 6 (self-test criteria) now carry an `Owner/Exit:` **pointer** to the authoritative v4.1.11 entries instead of a second, hand-written restatement (Grok Pass 1 b1/b6/b7/b8: the restatements diverged from the v4.1.11 wording — a fresh L-20260817-04/06 drift). Nothing renumbered, reordered, closed, or removed; the v4.1.11 list stays authoritative and the older list is kept for the record.
- Independent validation: the 3-pass Grok ladder runs on THIS polish before it lands — not on the CLI-bridge text that produced the ladder — the first non-self-referential use of the default CLI path. Outcome is claimed only in the Ladder line below; a clean run narrows v4.1.10 item (c), it does not close it (one run is not a track record).
- Ladder: COMPLETE — Pass 1 defects 8 bullets/7 applied (210s), Pass 2 false-green 8/3 (344s), Pass 3 adversarial 8/3 (294s); 24 bullets, 13 applied, 11 rejected, every bullet dispositioned, no empty pass; transport cli all three.

## v4.1.12 addendum — 2026-08-18 (Review-gate availability + verification pack)

retrieval_ref `LM-RET-2026-08-18T16:39Z-F` (orchestration-memory@dc82925d6b8a, repo@c8d8884; pass-3 record verbatim, CLAUDE.md + docs/GROK.md + HANDOFF-FORMAT + NIGHTLY-HYGIENE + LANDING-PROTOCOL + items 13/14 + Optimize addendum + lessons L-20260817-01…06 + journal records 43–45) — Fable gated, Opus executed; the execution unit took 5 infra bounces on a provider incident (Opus elevated 529s from 16:20 UTC), same unit each time, no work lost. Docs + stdlib tooling only, $0, no new dependency, no paid xAI API.

- ADDED `### Review-gate availability` to BOTH package files under `## Critique policy`, byte-identical (L-20260817-04/06): no PASS/CLOSED on significant work without its `grok_critique` records (1 routine / 3 ladder); CLI unavailable AND no browser session ⇒ **BLOCKED_ON_CRITIQUE** (work stages, nothing false-greens, queue clears only when critique runs and dispositions are journaled); the honesty clause that the browser fallback still needs a running Owner session on a running machine, so Owner PC off = both default paths down; the Owner accepted-risk waiver for temporary CLI-only operation (item 13) that AUTO-REOPENS when significant work waits while that machine is offline; a second independent transport stays Owner-escalated (Hard Rule 6 if credentials or spend).
- ADDED `## Review-gate availability (queue + honest fallback)` to `docs/GROK.md` — `critique_blocked` record shape (`target`, `waiting_since` that does not reset on retries, `staged`), the queue-clear procedure (oldest first, one `grok_critique` record per call, PASS only after the required records exist), and the `owner_accepted_risk` shape with its auto-reopen trigger — and AMENDED the Critique journal contract: silence on ANY bullet is not a decision, minors may share one batched reason but still one entry each, `len(applied)+len(rejected) == bullets_count` always.
- ADDED `tools/validate_journal.py` (stdlib, no network; `--journal` / `--open-items` / `--sections` / `--all`, `--strict`, `--self-test`): C1 every journal line parses and every `grok_critique` record carries the 12 required keys with its bullet arithmetic intact (empty ladder pass = FAIL unless honestly recorded `status` `"FAIL"`, then ADVISORY); C4 every numbered item in the LAST open-items section names an Owner and an Exit (ADVISORY unless `--strict`, struck-through items exempt); C5 both package files carry `## Critique policy` and `## Credit-Aware Routing` — presence only, and the output says so.
- ADDED `.github/workflows/verify.yml` (job `verify-docs`, push to main + pull_request, paths `docs/**` `tools/**` `.github/workflows/**`): validator `--all`, then specguard on both package files compared against the committed `tools/specguard_baseline.json` — the baseline records the REAL current findings (SYSTEM-CURRENT 3 [2 FAIL, 1 ADVISORY], SPEC 1 [0 FAIL, 1 ADVISORY]), so the gate fails on NON-baseline findings only and never pretends the package is clean — then a secret scan over `docs/ tools/ .github/` whose patterns are written so they cannot match their own definitions.
- ADDED the post-land CONTENT-check invariant to `docs/LANDING-PROTOCOL.md` (a named heading or string introduced by THIS commit must be found in the mirrored file; an advanced `modifiedTime` is not verification) and wired it into `docs/NIGHTLY-HYGIENE.md` steps c (validator alongside specguard) and g; pointers added to `CLAUDE.md` and one Owner-facing line to `docs/OWNER-QUICK-REFERENCE.md`.

**POST-LAND pass-0 critique of the LANDED c8d8884 Drive text** (cli, grok-4.5, exit 0, 185s, 6 bullets; journal record 46) — gate dispositions:

- **APPLIED b1** — the landed text carried a real contradiction: item 14 Exit says closure is "expected to close at this cycle's landing verification" while the Optimize bullet says a clean run "does not close it - one run is not a track record". RESOLVED: this closure supersedes both, closing NOW on the full evidence stack (ladder + per-bullet dispositions + landed SHA + Drive content check + this extra post-land pass).
- **APPLIED b2** — a closure must publish the landing SHA: this addendum cites **c8d8884** explicitly. The landed text could not contain its own SHA, which is exactly why the Exit prescribed citation in the next patch.
- **REJECTED b3** (caveat recorded, not waived) — the Owner order counts the optimize target as non-self-referential; the standing independence objection stays on the record, and the next significant ladder must use a non-governance / external target to retire that objection class.
- **APPLIED b4** — the `docs/GROK.md` example record shared the REAL retrieval_ref and could read as live evidence: it now carries `LM-RET-EXAMPLE-0000` and is labelled an illustrative example, not a real record (this commit).
- **APPLIED b5** (partial) — the Drive mirror cannot show commit boundaries, so a reader cannot tell what landed together: the closure note states that journal records 43–45 and the fixes they produced landed together IN c8d8884. The mirror still shows no boundaries; the note carries them.
- **REJECTED b6** — not a defect: Grok confirms the "Ladder: COMPLETE" aggregates (24 bullets, 13 applied, 11 rejected) match the per-pass journal rows 43/44/45.

**Item 14 CLOSED** on that stack — ladder records 43/44/45 (24 bullets, 13 applied, 11 rejected) · landed **c8d8884** · Drive CONTENT-verified 2026-08-18 · post-land pass-0 record 46 · Pass-3 reject-b5 quoted verbatim in the item itself with the independence caveat intact. Scope of this closure (v4.1.12 ladder Pass 3 b1/b5): closure of item 14 validates critique QUALITY on the default path only; Review AVAILABILITY remains open as item 13 — the Review gate is NOT autonomy-ready until item 13 resolves (headless transport or Owner decision). Item 13 stays OPEN and unwaived, its Owner/Exit wording corrected by ladder Pass 1 b6 — B1 is a second path, not a second machine; only a headless B3/C1 transport removes the SPOF, and it stays Owner-escalated under Hard Rule 6. Closure-decision record — all five Pass-3 rejections from journal record 45, verbatim:

- b3: "the 13/14 split is deliberate decomposition - 14 is critique-quality independence (self-reference), 13 is transport redundancy; each has its own exit and both cite v4.1.10 (b)/(c)"
- b4: "the narrowed closure already states ingest and enforcement are NOT closed; run-journal.jsonl IS the current Event Bus surface (JSONL/file spool per SYSTEM infra) - no renamed close"
- b5: "Owner order defined non-self-referential as not-the-CLI-bridge-text and this target is open-items governance prose; PRESERVED VERBATIM for the next gate - Grok argues the target is still same-system governance prose written by the same orchestration, so a truly independent target (external artifact or third-party review) would be stronger evidence before item 14 closes; closure stays a next-patch decision"
- b6: "legacy entries are explicitly record-only and the authoritative list is the live text; the date-pin exists for drift detection at review - a SHA pin on same-file prose adds noise without a checker, and that checker is open item 2 scope"
- b8: "pre-land invisibility is inherent to pre-land review; the landing gate verifies the GROK.md section is visible in the Drive mirror before any CLOSED claim survives the Summary Report (same as Pass 2 b8)"

Ladder: COMPLETE — Pass 1 defects 8/1 (237s), Pass 2 false-green 8/3 (150s), Pass 3 adversarial 8/3 (285s); plus post-land pass-0 6/4 (185s); every bullet dispositioned, no empty pass; transport cli all four.

## v4.1.13 addendum — 2026-08-18 (Efficiency mode + Cursor lane)

retrieval_ref `LM-RET-2026-08-18T21:52Z-H` (orchestration-memory@95ba531cca85, repo@680716a; CLAUDE.md + GROK.md + HANDOFF-FORMAT + LANDING-PROTOCOL + NIGHTLY-HYGIENE + REMAINING OPEN ITEMS 1–14 + v4.1.12 addendum + lessons L-20260817-01…06 + journal records 46–50). Owner order, 2026-08-18: *"fix the system now — implement Cursor into the system and use Fable 5 just as the bookends/orchestrator."* Docs, plus two small edits INSIDE the existing verification pack (ladder Pass 2 b5/b6) — $0, no new dependency, no new machinery. Fable gates this cycle; one Opus 5 execution unit built it.

- ADDED `### Efficiency mode (Fable bookends)` to BOTH package files under `## Critique policy`, byte-identical, 8 lines after the ladder (L-20260817-04/06): Fable at most twice per run (optional kickoff only when `docs/RUN-TEMPLATE.md` does not cover the task shape, plus the mandatory end gate), target ≤5% of run tokens on Fable; in-run critique dispositions PROVISIONAL until the gate ratifies them from `docs/LATEST-HANDOFF.md` + the journal (extends Review-gate availability, relaxes nothing); six and only six mid-run re-entry triggers; and the CURSOR LANE clause. Frequency, never depth.
- NEW `docs/EFFICIENCY-MODE.md` (the eight-step bookend flow with an owner per step, the Fable budget, the expanded re-entry list, what still costs Fable, and credit notes), `docs/RUN-TEMPLATE.md` (the one-paste activation text, the short GATE prompt, the CURSOR DISPATCH template), `docs/CURSOR-LANE.md` (setup checklist, dispatch procedure, PR review lane, pilot, honest limits), and `docs/LATEST-HANDOFF.md` (always the newest run's HANDOFF block, overwritten every landing; seeded here with a clearly-labelled RECONSTRUCTION of the v4.1.12 handoff from this file's v4.1.12 addendum + records 43–50 + `git show --stat 680716a`, with unrecorded fields marked unrecorded rather than guessed).
- ADDED one invariant to `docs/LANDING-PROTOCOL.md` — every landing updates `docs/LATEST-HANDOFF.md` in the SAME commit (it is the machine-findable CURRENT COPY of the baton; `docs/HANDOFF-FORMAT.md` stays the canonical FORMAT) — plus three pointer lines in `CLAUDE.md`. `docs/GROK.md` was NOT going to be touched, but ladder Pass 1 b2 found a live conflict and made it essential: the transport sentence said "the orchestrating Claude session (Fable) invokes the CLI", which bookends contradict, so it now reads "the orchestrating Claude session of the run — the execution session in efficiency mode, or the Fable surface in system-surgery runs". One sentence; no prompt block, record schema, or transport changed. `docs/run-journals/INTERCONNECT.md` also updated (Pass 1 b6): its Phase-2 note listed Cursor as deliberately deferred, which the Owner order supersedes.
- BUNDLE FACT (verified 2026-08-18): Cursor Ultra is included at $0 while the Owner's SuperGrok Heavy stays active (`cursor.com/help/grok-bot/supergrok-heavy`; one Grok account pairs one Cursor account). Cursor Ultra background agents can work a GitHub repo on branches and open PRs. Cursor output is a SECOND BUILDER POOL, never a gate: PRs only, UNVERIFIED under Hard Rule 3, merged only after `verify-docs` CI + a Grok critique of the diff + a Claude gate merge (the merge is the live write — Hard Rule 1), never a push to `main`, hard stops binding every lane.
- CI LEG PRE-BUILT, confirmed by reading `.github/workflows/verify.yml` at 680716a: `verify-docs` already triggers on `pull_request` (paths `docs/**`, `tools/**`, `.github/workflows/**`) as well as pushes to `main`. Nothing was added to CI this cycle. Honest caveat recorded in `docs/CURSOR-LANE.md`: a PR touching only paths outside that filter runs no checks and needs hand review.
- OWNER SETUP STATE, updated mid-cycle 2026-08-18: Chris reports "cursor is connected" — the Grok → Cursor pairing and the GitHub connection to `chrispcariello/autonomy-system` are both DONE per Owner report (one transient "Could not resolve default branch" error during the connect, which then resolved). Both were Owner-hands account/credential steps under Hard Rule 6; no Claude session performed them, and the system has not independently verified either. **Cursor lane status: CONNECTED, ACTIVATION PENDING — connection is not activation; the first pilot PR through the full review lane is the activation evidence, and open item 15's Exit is unchanged by the connection.** The pilot is defined (one bounded docs-only task) and has NOT been run; Ultra's usage limits are UNKNOWN and stay recorded as unknown until a run measures them.
- xAI API KEY, recorded as AVAILABLE-not-wired: the Owner already holds an xAI API key with loaded credits (created 2026-07-28 for his PokeLedger advisor). It is NOT wired into this system — no bridge, no workflow, no code path. It bears on open item 13: the Owner's existing key makes the COST argument against roadmap C1 (a headless critique transport) moot, but wiring it is still credentials + spend and remains a separate Owner-ordered step. Item 13 stays OPEN and unwaived.
- OPEN ITEMS: none closed this cycle. ADDED item **15 — Cursor lane activation** (setup DONE per Owner report; remaining Owner actions: scope the GitHub grant — flagged URGENT because that grant is LIVE NOW while the lane is unactivated — and decide branch protection; Exit: the first Cursor PR passes the full review lane and merges, with a journaled `cursor_dispatch` record). Nothing renumbered, reordered, or removed; count is now 15 listed, 13 open.
- MEASURABILITY + RATIFICATION ARTIFACT (ladder Pass 2 b3/b4): the ≤5% Fable-token target has no meter — nothing here reads per-model token spend — so the block and `docs/EFFICIENCY-MODE.md` now state that plainly and track the **Fable phase count** (0/1/2) instead; an unmeasured target is a goal, not a claim. Ratification now leaves an artifact: a `gate_ratification` journal record (`target`, `dispositions_reviewed`, `overturns`, `verdict`, `fable_phases`, `retrieval_ref`) is REQUIRED before any PASS/CLOSED claim from an efficiency-mode run. Nothing yet forces that record to EXIST for a given run — that is open items 2 and 5, and it is said so in the doc rather than implied away.
- THE VERIFICATION PACK MAINTAINS ITSELF (ladder Pass 2 b5/b6, the only code in this cycle): `tools/validate_journal.py` now FAILS on a `cursor_dispatch` record missing `ts`/`type`/`task`/`branch_or_pr`/`outcome` or a `gate_ratification` record missing `ts`/`type`/`target`/`dispositions_reviewed`/`overturns`/`verdict`, with two new seeded self-test fixtures (`--self-test` 11/11 PASS); and `.github/workflows/verify.yml` drops the PATH FILTER on `pull_request` only (pushes to `main` keep theirs), so every PR runs the checks whatever it touches — previously an out-of-scope PR could have merged green-by-absence. Both edits are inside the pack that already gates this repo: a new lane whose review leg can be bypassed by file placement is not a review leg.
- LANDING + SCOPE GUARDS (Pass 2 b7, b6): `docs/LANDING-PROTOCOL.md` now says Tiers 1 and 2 are for CLAUDE-AUTHORED work only and Cursor-lane work reaches `main` ONLY as a reviewed PR merge; dispatches are scoped to `docs/**` and `tools/**` by the template and an out-of-scope PR is closed without merge, not fixed up. Honest limit, stated in both files: this repo has NO branch protection, so the Owner scoping Cursor's GitHub access plus a manual Files-changed check by the gate is the whole guard until items 2/5 mechanize it. Pass 2 b1 was REJECTED: item 15's Exit already reads "the first Cursor PR passes the FULL review lane … and the dispatch carries a `cursor_dispatch` journal record with `outcome` `merged`", which pairing alone cannot satisfy.
- STAGING vs CANON + CAP LIMITS (ladder Pass 3 b1/b3/b5): the block now says the ≤5% cap governs Fable SURFACE APPEARANCES only, never the gate's authority, and the six re-entry triggers are EXEMPT — the cap always yields to a triggered re-entry. A new block bullet frames branch writes against Hard Rule 1: **branches are STAGING, not canon** — HR1 protects CANONICAL state (`main`, tags, released docs), a `cursor/*` branch is Event-Bus staging, and the MERGE is the canonical write, performed only by the Claude surface (mirrored in `docs/CURSOR-LANE.md`). `docs/EFFICIENCY-MODE.md` adds the conservative-direction rule: ratification may turn a PASS into FAIL or re-open an item, but can NEVER turn a FAIL or a missing critique record into a PASS — upgrading needs a NEW critique record, so a thin late ratification cannot wash a ladder. Pass 3 rejections: b2 (the block already exempts the gate and labels 5% goal-not-claim), b4 (prompt-space artifact — the six triggers are in the draft; the pass-3 prompt omitted them for char budget), b8 (the proposed cuts are the honesty markers themselves).
- THE WIRE IS NOT DARK (Pass 3 b6/b7): the GitHub grant is LIVE NOW although the lane is unactivated, so scoping it is URGENT, not cosmetic — "no dispatch yet" limits what the lane has DONE, not what the credential CAN do. Item 15 and `docs/CURSOR-LANE.md` now say so, and branch protection becomes an Owner DECISION inside activation: (a) a GitHub ruleset requiring PRs with a bypass allowance for the Owner's own landing paths, or (b) manual-gate merging plus a strictly scoped grant. PENDING — nothing was configured this cycle. The stale v4.1.11 "Free tools adopted now vs deferred" entry for Cursor gets an appended *superseded 2026-08-18 by Owner order* note; the original deferral text is left intact as the record.
- SCOPE HONESTY: this cycle is itself system surgery — a Hard-Rules-adjacent, multi-file package change — and is therefore EXEMPT from the mode it installs. Rule changes stay Fable-gated ladder work by definition. Efficiency mode is claimed as WRITTEN, not as PROVEN: no run has yet executed under it, and the ≤5% Fable-token target is a target with no measurement behind it.

**Ladder: COMPLETE** — Pass 1 defects 8/7 (288s), Pass 2 false-green 8/7 (163s), Pass 3 adversarial 8/5 (381s); 24 bullets, 19 applied, 5 rejected, every bullet dispositioned, no empty pass; transport `cli` all three. Journal records 52, 53 and 54.

## Cursor lane activation — 2026-08-18

retrieval_ref `LM-RET-2026-08-18T21:52Z-H`. Same-day sequel to the v4.1.13 addendum: the lane documented that morning is now ACTIVE, on evidence.

- ACCESS, fixed by the gate courier before dispatch: the Cursor GitHub app was scoped from ALL repositories to `autonomy-system` only, and ruleset **protect-main-cursor-lane** was created (Active; `main` requires pull requests; bypass Repository admin + Claude app, always-allow; only that one rule — deletion and force-push toggles left off deliberately). The gate merge 5ce249f proves the bypass works for admin pushes, so the Owner's Tier 1/Tier 2 landing paths are not blocked.
- PILOT: agent `bc-88581fe2-6b2c-4b2c-b6d0-eae91640652a` → **PR #2**, `docs/CURSOR-PILOT.md` +25, in scope, with its own retrieval_ref. `verify-docs` **SUCCEEDED on the PR — run 32196858563**, the first observed CI conclusion in this lane. Grok batched routine critique (cli, exit 0, 150s, 6 bullets, 6 applied, record 55). Gate merged as **5ce249f**. Open item 15 CLOSED on that stack; dispatch records 56 and 57 ride this commit.
- **PR #1 CLOSED, not merged** — `.cursor/environment.json` is outside the `docs/**` + `tools/**` scope and the PR carried zero completed checks. Content re-landed Claude-authored here with Grok b6 hardening: `pip install --quiet google-auth google-api-python-client`, **no `--upgrade`**, matching `sync-docs-to-drive.yml` so the agent environment cannot silently jump a major version.
- HONEST NOTES, none of them blockers, all of them unmeasured: Cursor ran the pilot on **Auto** (its own usage-limits banner was showing), so nothing here measures Ultra's real limits; Cursor's GitHub-connected workflows showed a **degradation banner** during the run, which did not block the PR or the checks; and PR #1's description carried a **stale 9/9 validator self-test claim** — the current count is 11/11, and the stale number is recorded rather than quietly corrected.
- WHAT THIS DOES NOT PROVE: one pilot is not a track record, no Cursor PR has yet been REJECTED on quality (only on scope), and the lane's throughput, cost ceiling and failure modes remain unknown. `docs/CURSOR-LANE.md` keeps its honest-limits section unchanged for exactly that reason.

## v4.1.14 addendum — 2026-08-19 (Autopilot lane)

retrieval_ref `LM-RET-2026-08-19T09:22Z-A` (live clone at base `59aaa51`; `docs/LATEST-HANDOFF.md` as landed 2026-08-18T23:33:28Z, parent `5ce249f`; CLAUDE.md + EFFICIENCY-MODE + RUN-TEMPLATE + GROK + HANDOFF-FORMAT + LANDING-PROTOCOL + both package files + REMAINING OPEN ITEMS 1–15 + `tools/validate_journal.py` + journal records 51–57 + lessons L-20260817-01…06). Owner order: one paste to Fable, which plans, spawns an Opus crew, stays out of the room while the crew builds, and gates on return — routing a fix order back to the crew until the work is ratified, with a hard 3-loop ceiling. Docs only, $0, no new dependency, no new machinery. **This cycle is itself the first run of the shape it installs:** Fable planned it and spawned this Opus execution crew, which self-briefed from the repo and ran to a HANDOFF without checking in. The gate is PENDING; nothing here is claimed PASS or CLOSED.

- NEW `docs/OWNER-FLOW.md` — the Owner-facing map, in lay language, of all four lanes: (1) routine (START card to Opus, GATE card to Fable later), (2) plan-touch (PLAN prompt to Fable, work order back, START card to Opus, gate in the same chat), (3) AUTOPILOT (one paste, Fable plans and spawns and gates, verdicts RATIFY / FIX / BLOCK with the bounded fix loop), (4) system surgery (Fable live, rare and expensive). Also: the one-card principle (`docs/RUN-TEMPLATE.md` holds every prompt the Owner will ever paste), who bills what (Opus = same Claude wallet at a much cheaper meter; Cursor = free with SuperGrok Heavy, PRs only through CI + Grok + a Claude gate; Grok = the Grok subscription, zero Claude usage; Fable = only its speaking moments, with cached re-reads on re-entry) **with the explicit caveat that no per-model meter exists**, the desktop-hands requirement, the `BLOCKED_ON_CRITIQUE` parking rule, and an honest note that there is **no franchise kit for new projects yet** — it is a roadmap item, and the working path today is a START/AUTOPILOT task line plus a `<project>-live` clone.
- ADDED `docs/RUN-TEMPLATE.md` blocks **4 — PLAN** (plan only: return a work order with goal, run breakdown, exact THE TASK lines, critique depth, done-criteria, risks — then stop, execute nothing) and **5 — AUTOPILOT** (the one paste: classify-and-refuse-if-surgery, plan a standalone work order with explicit scope and stop conditions, spawn the crew, stay out of the room, gate on return, fix-loop with routing and a re-gate, land the `gate_ratification` record through a scribe agent, report the verdict). The file's intro now says five blocks, not three.
- ADDED `### Autopilot lane` to `docs/EFFICIENCY-MODE.md`, and ONE identical line to the byte-identical `### Efficiency mode (Fable bookends)` block in BOTH package files (sha256 of the block matches across both files; `## Critique policy` and `## Credit-Aware Routing` intact in both). Both files → **v4.1.14**: titles, SPEC `**Date:**` 2026-08-19 and intro line, history rows in both, exactly one `(this document)` per file on the new row after the em dash.
- **fable_phases, for this lane:** an autopilot run counts as **2** (plan + gate) **plus 1 per fix loop actually run**, where one fix loop means the fix order AND the re-gate that closes it. The loop count is therefore always `fable_phases - 2` — deliberately no second counter to disagree with it. Every fix loop is journaled. The conservative overturn rule is unchanged and now binds the re-gate explicitly. System surgery still requires a live Fable run and Fable **refuses the spawn** for it.
- **WHAT THE LADDER CHANGED, because it was not cosmetic.** Pass 1 fixed the `RATIFY`-vs-`verdict PASS` vocabulary clash (mapped once: RATIFY ⇒ PASS, FIX/BLOCK ⇒ FAIL) and the loop boundary (the 3rd fix loop's re-gate is the LAST gate; no 4th fix order). Pass 2 found the sharpest hole: the fix-loop critique requirement bound only RE-gates, so a **first-gate RATIFY over a thin or absent ladder was legal** — the first gate is now held to the same standard, and the exit for item 16 now requires a cited evidence stack rather than a correctly-spelled row. Pass 3 forced the two honesty statements this lane most needs — **the gate is POST-LAND** (it stops the bad CLAIM, not the bad commit; the pre-land protection is the crew's own ladder plus the validators) and **nothing watches the crew between spawn and return** (no supervisor, no heartbeat, no timeout; a crew that never returns is evaluated retroactively) — plus the pre-spawn surgery refusal, the rule that a **FIX routed to Grok orders a critique pass and NEVER a write**, and the no-rollback rule on BLOCK (fast-forward-only history, so unratified landed work is NAMED as unratified rather than reverted).
- REJECTED, with reasons on the record (journal records above): Pass 2 b7 (`fable_phases` is gameable) — the known unmeasured-proxy problem, and a cross-check is new machinery outside a docs-only cycle; a second counter was deliberately *removed* by Pass 1 b4 to stop two numbers disagreeing; folded into open items 2 and 5. Pass 2 b8 (token-thrift claim has no instrument) — the text already discloses exactly that; nothing to fix. Pass 3 b8 (worst field failure) — the composite of b1/b2/b3/b5, each dispositioned individually.
- ROUTING: **cursor — not dispatched this run.** Small docs diff; the review-lane latency (branch → PR → CI → Grok diff critique → gate merge) is unjustified for it. Recorded so the non-use is a decision rather than an omission.
- OPEN ITEMS: none closed this cycle. ADDED item **16 — Autopilot lane**, appended at the end of the authoritative v4.1.11 list; nothing renumbered, reordered or closed. Count supersedes every earlier line: **16 listed, 13 open**. Its exit is the first RATIFIED autopilot `gate_ratification` record on `main`, and this run's crew deliberately does NOT close it — the gate scribe does.
- HONEST EXCEPTION, pre-existing at base and NOT introduced here: `python3 tools/validate_journal.py --all --strict` exits 1 at base SHA `59aaa51` on legacy open item 10, which cites no record number and no commit SHA. The v4.1.12 cycle left that ADVISORY honest and un-backfilled on purpose, so this cycle did not silently reverse another cycle's recorded decision to make its own run look clean. `--all` (what CI runs) exits 0 before and after; the gate is flagged to decide whether item 10 should be backfilled from journal records 24/28/29.
- NOT CLAIMED: no gate ratification exists, no autopilot run has ever been gated, the fix loop has never executed and the 3-loop ceiling has never been reached. The lane is **WRITTEN, not PROVEN**.

**Ladder: COMPLETE** — Pass 1 defects 8/8 applied (53s), Pass 2 false-green 8/6 (38s), Pass 3 adversarial 8/7 (44s); 24 bullets, 21 applied, 3 rejected, every bullet dispositioned, no empty pass; transport `cli`, `exit_code` 0 all three. All dispositions stay provisional until the Fable gate ratifies them.

## v4.1.15 addendum — 2026-08-19 (Owner routing directive + mandatory usage receipts)

retrieval_ref `LM-RET-2026-08-19T10:26Z-A` (live clone fast-forwarded and verified at base `3af3ca9b3983a4bdb459942aa40153ee629f4cf1`; CLAUDE.md + both package files incl. the existing `## Credit-Aware Routing` sections + EFFICIENCY-MODE + RUN-TEMPLATE (5 blocks) + OWNER-FLOW + GROK + HANDOFF-FORMAT + LANDING-PROTOCOL + REMAINING OPEN ITEMS 1–16 + `tools/validate_journal.py` schemas + journal records 60–62 + lessons L-20260817-01…06 and L-20260819-01). **SURGERY-CLASS, and classified as such honestly:** this cycle edits ROUTING POLICY and BOTH package files, so it is lane 4 by definition — Fable was LIVE for it, authored the directive text verbatim, and gates the result; it was not and could not be autopiloted. Owner order, 2026-08-19, summarized: track usage after each task so Fable 5 credits are provably not wasted; route coding to Cursor when possible (Cursor Ultra is bundled with SuperGrok Heavy, with large included usage); use the ample Grok Heavy capacity; Claude is the meter to conserve. Docs only, $0, no new dependency, no new machinery.

- ADDED to `## Credit-Aware Routing` in **BOTH** package files, byte-identical: the **Owner routing directive (standing, 2026-08-19)** — Claude usage is the scarce resource and is conserved first; above-trivial repo-based coding routes to **Cursor** background agents BY DEFAULT when it fits the PR lane (zero Claude credit); **Opus** keeps trivial edits, canonical writes and merges, Owner-machine hands, session-tool work and anything Cursor cannot reach; **Grok** capacity is used liberally — critique always, plus drafting, research and large-text summarization through the Grok CLI as UNVERIFIED input to a Claude gate, Hard Rule 3 unchanged and Grok never writing to the repo; **Fable** appears only at bookends and gates; and **every run ends with a USAGE RECEIPT composed by the gate**. sha256 of the Fable-authored directive block: `980cd8d984a0e6918c6efb1a620feee97110a6ee05670a766d722c1edc41904e` — IDENTICAL in both files. A ladder-added SCOPE paragraph (Pass 3 b2/b6/b7) rides it, also byte-identical in both; sha256 of the full added region: `689c9fb762d287d59e1a33768be1db1ca68c4aa2cd79ac190d87f7efd56a0196`.
- ADDED to `docs/RUN-TEMPLATE.md`: the **AUTOPILOT** block (5) plan step must now state the ROUTING SPLIT (Cursor / Opus / Grok) and give a one-line reason whenever above-trivial repo coding is NOT routed to Cursor; the autopilot's final step and the **GATE** block (2) both require the verdict message to the Owner to END with the USAGE RECEIPT, reconciled against the run's `grok_critique` / `cursor_dispatch` / `gate_ratification` records.
- ADDED to `docs/EFFICIENCY-MODE.md` (Autopilot lane): the receipt requirement with its reconciliation rule and its blind-spot list, the Cursor-first coding default with its exclusions and fail-closed tie-break, and the Grok drafting/research lane bounded by the trust boundary.
- ADDED to `docs/OWNER-FLOW.md`: a lay-language **Receipts** section (what each crew cost in tokens, how many Fable moments, Grok passes, Cursor dispatches, where the work was sent; the app's usage settings hold the true meter; receipts are the per-run proof, and what they miss). The who-bills-what passage now carries the Cursor-first coding default and the Grok drafting lane.
- Both files → **v4.1.15**: titles, SPEC `**Date:**` 2026-08-19 and intro line, history rows in both, exactly one `(this document)` per file on the new row after the em dash. The byte-identical `### Efficiency mode (Fable bookends)` block was **NOT touched** — sha256 `9a7fcfd019c5e691395e17b669b5d63bcc58a9b298a17d6b2a4f71f2fba0f956`, the same value in both files at base `3af3ca9` and after this patch (heading + the nine bullets; the sentence that follows the blank line differs between the two files by design and always has).
- **WHAT THE LADDER CHANGED, because it was not cosmetic.** Pass 1 bounded the Cursor default away from surgery/package/canonical work, forced the receipt to RECONCILE against journal records rather than assert, named its blind spots, and turned the non-dispatch one-liner into a journaled routing note the gate may REJECT. Pass 2 killed the measurement theatre: zeros are banned in favour of `n/a`, reconciliation is stated to prove only internal consistency, `fable_phases` is admitted to have no independent witness, the "anything Cursor cannot reach" clause is defined as CAPABILITY and not schedule so deadline pressure must be recorded as speed, Grok drafting is bounded because the rewrite tax exceeds the saving on short text, and item 17 can no longer be closed by a ceremonial zero-spawn run. Pass 3 found the two that would have hurt most — the directive readable as a **builder charter** demoting the model split and Hard Rule 1 (now answered by the identical SCOPE paragraph: routing preference only, no authority moves, no credit state buys a thinner gate, Cursor-first IS "standard routing" at every threshold mode) and the **hot-path self-deadlock** (dispatching a broken validator's fix into the lane that waits on that validator; hot-path repairs now stay with Opus) — plus the governance-text carve-out that stops "large-text summarization" becoming a doorway for Grok-authored rulebook prose.
- **DISPOSITIONS: 24 bullets, 24 APPLIED, 0 REJECTED — and that is flagged to the gate rather than presented as a good score.** A ladder with no rejections is either an unusually apt critique or an insufficiently sceptical crew; the gate should read the three records (63/64/65) and overturn anything that was accepted too easily. One partial is recorded honestly inside its APPLY: Pass 2 b8 asked for an expected-versus-actual "credit saved" field, which was deliberately NOT built — the counterfactual is unmeasurable and inventing it is the estimation defect this patch exists to prevent — and a ROUTING LINE plus an explicit "activity, not savings" statement was built instead. Pass 3 b4's premise that the tie-break creates unrollbackable history is inaccurate for the Cursor lane (a PR is not landed until a Claude gate merges it, and PR #1 was closed unmerged); the useful half was applied and the correction written into the text.
- ROUTING for this cycle: **cursor — not dispatched.** This is policy text authored by the gate surface in a Fable-live surgery lane; dispatching policy wording to an outside PR lane adds review latency with no credit saving, and the new directive itself excludes surgery and package-file work from the Cursor default. Recorded so the non-use is a decision rather than an omission — and it is the first exercise of the rule this patch installs.
- OPEN ITEMS: none closed this cycle. ADDED item **17 — Owner routing directive + mandatory usage receipts**, appended at the end of the authoritative v4.1.11 list; nothing renumbered, reordered or closed. Count supersedes every earlier line: **17 listed, 13 open**. Its exit is the directive present and identical in both package files on `main` AND a gate-composed usage receipt delivered on a ratified run; this run's crew deliberately does NOT close it.
- HONEST EXCEPTION, pre-existing at base and NOT introduced here: `python3 tools/validate_journal.py --all --strict` exits 1 at base on legacy open item 10, which cites no record number and no commit SHA. That ADVISORY was left honest and un-backfilled by the v4.1.12 cycle on purpose and this cycle does not reverse another cycle's recorded decision to make its own run look clean. `--all` (what CI runs) exits 0 before and after; `--self-test` is 11/11.
- NOT CLAIMED: **no usage receipt has ever been delivered.** The gate for this very run is the first opportunity to produce one, and until it does, item 17 stays open. Nothing mechanically enforces receipt presence, receipt truthfulness, the routing split, or the rejection of a boilerplate non-dispatch reason — all of that is gate discipline pending open items 2 and 5. `specguard` output is byte-for-byte identical before and after on both package files (2 FAIL / 1 ADVISORY on FOR-CLAUDE, 1 ADVISORY on SPEC — all pre-existing, open item 1 and the known heuristic noise).

**Ladder: COMPLETE** — Pass 1 defects 8/8 applied (84s), Pass 2 false-green 8/8 (99s), Pass 3 adversarial 8/8 (124s); 24 bullets, 24 applied, 0 rejected, every bullet dispositioned, no empty pass; transport `cli`, `exit_code` 0 all three; prompt files `C:\Users\chris\grok-work\r15p{1,2,3}.txt` with `PROMPT_CHARS` 2861 / 3179 / 3120 echoed to prove full delivery (all three exceed the 1900-char guideline; the echo is the compensating control). All dispositions stay PROVISIONAL until the Fable gate ratifies them.


## v4.1.16 addendum — 2026-08-19 (speed pack + lane auto-briefings)

retrieval_ref `LM-RET-2026-08-19T11:28Z-A` (live clone `C:/Users/chris/autonomy-system-live` fast-forwarded and verified at base `4949bbc8cf04d7bdba85612e4920522c9866c51b`; CLAUDE.md, both package files incl. `## Credit-Aware Routing` and `### Efficiency mode (Fable bookends)`, EFFICIENCY-MODE, RUN-TEMPLATE all five blocks, GROK, CURSOR-LANE, HANDOFF-FORMAT, LANDING-PROTOCOL, PATCH-NOTES REMAINING OPEN ITEMS 1–17, LATEST-HANDOFF, `tools/validate_journal.py`, `.cursor/environment.json`, lessons `L-20260817-01…06` and `L-20260819-01`, journal records 62–67) — **SURGERY-CLASS, Fable live and authoring**: this cycle changes gate procedure and self-brief rules, so it could not be autopiloted and was not.

**What changed**

- ADDED `tools/gen_brief.py` — stdlib, deterministic, NO wall-clock value anywhere in its output, so re-running it on an unchanged tree is a byte-for-byte no-op. It reads fifteen canonical sources and writes three generated files: `docs/BRIEF-PACK.md`, `docs/GROK-CONTEXT.txt` and repo-root `AGENTS.md`. Missing, unreadable or empty source = loud failure, exit 2 — a brief generated from a hole is worse than no brief, because it looks current.
- `docs/BRIEF-PACK.md` carries the Hard Rules, the model split, the critique ladder, Review-gate availability, the routing directive and the shared Efficiency-mode block **EXTRACTED VERBATIM** from `docs/SYSTEM-CURRENT.md` — paraphrase is the drift vector a brief pack would otherwise introduce, so the generator copies bytes rather than summarising. The record-schema summary is read out of `tools/validate_journal.py`'s own constants, so it cannot drift from the checker that enforces them.
- `docs/GROK-CONTEXT.txt` is the snapshot prepended to EVERY Grok prompt file. Its constraints are ENFORCED by transformation and then asserted: pure ASCII, zero double quotes, zero apostrophes, no line beginning with a dash, ≤1500 chars. The FIXED authority lines are never truncated — if they would not fit, generation FAILS loudly; only the variable last-land line is trimmed, at a word boundary, and a trim is marked `[TRUNCATED]` rather than hidden.
- `AGENTS.md` (repo root) is the standing briefing for Cursor and other outside agents: scope (`docs/**` and `tools/**`, PR only, never `main`, out-of-scope PRs closed), conventions (journal is Claude-only; never touch PATCH-NOTES or LATEST-HANDOFF from the PR lane; generated files are outputs), and a pointer to the pack. It is a briefing, not a permission system, and says so.
- `tools/validate_journal.py` gains check **C6**, wired into `--all`: every MANIFEST source and both other generated files are re-hashed, any mismatch is a FAIL naming the stale files, the MANIFEST-DIGEST is recomputed from the rows, and a `SELF-DIGEST` catches a hand-edited pack. An ABSENT pack is a clean skip by design. Self-test grows **11 → 18** cases, including a fresh-pack pass, a stale-source fail, a hand-edited-output fail, a deleted-source fail, a hand-edited-pack fail, a manifest-amputation fail and the clean-skip case — a staleness check nobody has proven can fail is not a check.
- RULES integrated: SELF-BRIEF VIA PACK, the REGENERATION RULE, the GROK CONTEXT RULE, LEAN SCRIBE, BATCH GATING and PARALLEL CREWS (fan-out) — `docs/EFFICIENCY-MODE.md` → The SPEED PACK, `docs/RUN-TEMPLATE.md` blocks 1/2/3/5, `docs/GROK.md`, `docs/CURSOR-LANE.md`, and ONE byte-identical line added to the shared `### Efficiency mode (Fable bookends)` block in both package files (block sha256 `be24c5ef2c2d406c1ee8fe37e6e1b9c0bdefa0f869f62e19c562ae359cc31c64`, equal across the two after the edit).
- ROUTING for this cycle: **cursor — not dispatched.** Surgery-class by classification, so the Cursor default does not reach it at all; and the diff is interdependent across a generator, the validator it imports, four rule documents, both package files and the open-items board, which is the case rule 3f (parallel crews) names as the WRONG shape for fan-out. One Opus crew, deliberately, as a live worked example of when not to split.
- LEAN SCRIBE is written but **not yet in force for this run**: it takes effect once this cycle is ratified, so this run's own scribe follows the law in force at landing and verifies the Drive CONTENT strings at the gate.
- CORRECTION, PATCH-NOTES-ONLY and deliberately so: the v4.1.16 prose was written BEFORE the Grok ladder finished, and the ladder's own fixes made some of its counts stale. The TRUE landed numbers, checkable against the artifacts at `a1097a8`: `tools/validate_journal.py --self-test` runs **18** cases (Pass 3 b4 added the manifest-amputation fixture) and `tools/gen_brief.py` reads **fifteen** canonical sources (Pass 1 b4 and Pass 2 b3 added `docs/OWNER-FLOW.md`, `docs/NIGHTLY-HYGIENE.md` and `docs/lessons/lessons.jsonl`). Three prose sites were stale: **"11 to 17 cases"** in the v4.1.16 history row of BOTH package files, and **"11 → 17"** plus **"twelve canonical sources"** in this addendum. The addendum sites are corrected above; **the two package history rows are deliberately NOT edited and still say 17**. Reason, on the record: a two-package-file edit is SIGNIFICANT by the written test ("any multi-file package change"), and "the definition of significant may only be narrowed by an explicit Owner order recorded in PATCH-NOTES — never by session judgment" — so correcting them cheaply would have been exactly the forbidden narrowing. This entry is the correction instead: single-file, no rule/routing/safety text, genuinely routine. The stale row is therefore a KNOWN, RECORDED inaccuracy in the tagged changelog rather than a hidden one, and correcting it is available to any later run willing to pay the ladder. Tag `v4.1.16` also stays on `a1097a8` and is not retargeted — not because any sentence bans moving a tag (it does not), but because `main`, tags and released docs are canonical state under Hard Rule 1 and the landing invariants ban force-push and rebase of landed history; a sealed changelog that stays byte-stable is worth more than a tidier one. `docs/BRIEF-PACK.md` embeds the package history row verbatim, so the pack correctly carries the frozen 17 too — the pack is faithful to its source, which is the property it is built to have. HOW IT WAS FOUND: by reading the MIRRORED file during the post-land Drive CONTENT check, which is exactly what that check exists for, and it is worth recording that the check earned its keep on its first run under the new rules.
- HONEST EXCEPTION, pre-existing at base and NOT introduced here: `python3 tools/validate_journal.py --all --strict` exits 1 on legacy open item 10, which cites no record number and no commit SHA. `--all` (what CI runs) exits 0 before and after.
